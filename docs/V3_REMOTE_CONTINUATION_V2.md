# Verified continuation for the qualified PMX clock correction

10 September 2026. Full preflight [34460574221](https://github.com/a-a-k/telemetry-availability-identification/actions/runs/34460574221) is running from tag `v3-preflight-771605-source`, source `27db98d32b683b14969f2734f19cf344c95ae1a7`. Its source CI 34460277709 passed. This coordination version preserves all 356 frozen experiment files and the completed [PMX repair qualification](milestones/PMX_CLOCK_PROGRESS_QUALIFICATION_V1_RESULT.md).

The new admission workflow accepts only that terminal original preflight. It verifies its repository, source, workflow, attempt and successful source CI, retains the exact 28 compact artifacts, and applies the frozen v3 auditor to all six campaigns, 200 method slots, 48 oracle records, the qualified derived PMX binary, role seals, replay and actual read order. Forecast error or superiority is never an admission condition. No additional experiment is inserted between this full preflight and main.

Once those checks pass, the coordinator commits the compact proof and generic admission record `configs/v3_main_admission_v1.json`, bound to the v3 protocol and 356-file execution lock. It creates the unique tag `v3-main-771601-v3-admitted` and verifies CI at that admitted source, then dispatches the existing 240-campaign main workflow once. The exact run, tag, source and CI are retained in `docs/evidence/v3-main-dispatch-v3.json`. Existing runs are checked before any dispatch; uncertain responses are not blindly retried. Conflicting pushes and changed tags fail explicitly. Source tags allow editorial work to continue without changing running experiments.

The terminal collector accepts only the persisted admitted main run. It retains the 964 exact compact artifacts or explicit absences, verifies source/tag/admission and the 240/800/8000 planned census, then applies the frozen v3 main auditor to every campaign, including PMX binary provenance. It exports existing remote metrics into publication tables without fitting, replay or bootstrap. Scientific interpretation and the completed manuscript remain necessary after collection. Every unsupported case, technical failure and empty contrast stays visible. Full application observations, models and solver payloads remain remote.

Admission and collection create complete 30-criterion snapshots (iterations 056 and 057), based on the current iteration 055. The old workflows and failed v1/v2 evidence remain unchanged. The workflow trigger accepts the actual tagged preflight and checks its exact run ID; it does not depend on a main-branch filter.

Five bounded artificial coordination tests cover failed evidence, incorrect provenance, duplicate and uncertain dispatches, and unrelated completions. Both new workflows pass actionlint 1.7.12. Existing frozen auditors and reporting controls are reused.
