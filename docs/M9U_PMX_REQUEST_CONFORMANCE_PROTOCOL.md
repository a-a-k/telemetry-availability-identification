# M9U: external-request PMX conformance before application forecasts

Frozen before execution on 8 September 2026. The current user request explicitly
authorizes completing the independent comparison. M9U establishes a prerequisite;
successful artificial controls do not establish application predictive validity.

The six fixed cases each contain ten calibration requests and are extracted twice
by the pinned author PMX binary with the author's unchanged CPU/options defaults
except the input path. The accepted M9S artifact is downloaded and byte-verified
before execution. The original M9S solver harness, analyzer commit, configuration,
target platform and two solver passes remain unchanged.

An explicitly synthetic external-request wrapper carries the recorded semantic
outcome. It is an additional endpoint input, not discovered native instrumentation.
Its interval encloses all selected native spans, one microsecond beyond either
edge; this structural interval is not a latency measurement. Original native
times, labels, parents, errors, links and attributes are retained in the audit.
Native HTTP method/path attributes qualify otherwise colliding operation names;
queries are excluded. A declared single host is used for these artificial controls.
Missing physical deployment or replica evidence is not inferred from this host.

OTel uses explicit SERVER boundaries. The unmarked control uses service changes
and native roots, contracting unselected ancestors with explicit provenance.
Multiple roots attach to the disclosed wrapper; missing traces have a wrapper only
and are counted as having no original trace coverage. Invalid native traces are
rejected and retained; incomplete extraction cannot qualify by dropping requests.

PMX's operation probabilities must equal inclusive error counts before bridging.
The native-inclusive variant preserves them. The conditional-local sensitivity
uses parent errors with no direct-child error divided by invocations with no
direct-child error, independently of the proposed model's fits or topology.
It requires a positive conditioning denominator and no observed child error with
parent success. This observational check does not establish independence. An
unsupported operation causes variant abstention, not silent probability repair.

| Control | Inclusive success | Conditional-local success | Purpose |
|---|---:|---:|---|
| nested_propagated | .576 | .8 | inclusive failure duplication across wrapper and native parents |
| compound_http | .81 | .9 | three HTTP roots for one user operation |
| absent_trace | .8 | .8 | two failed external requests, no native spans |
| unmarked_service_boundary | .576 | .8 | same-service client contraction, unmarked native roles |
| repeated_http_and_collision | .6561 | .729 | two cart calls and one checkout call, preserved multiplicity |
| swallowed_child | .9 | unsupported | native parent succeeds despite a child error |

The repeated-call control has correlated observed errors and empirical external
success .9. Its independent serial-composition oracle is deliberately different;
passing this oracle is a solver/structure result, not an accuracy claim.

The seven reference-resolved PCM structure/error checks and the exact reconstructed
trace/span/operation/parent/time inventory must pass. Every supported variant is
solved twice for each of the two extractor repetitions: 22 models, 44 records.
Acceptance requires all 44 probability oracles within 1e-12, normalized physical
mass, complete physical-state evaluation, unique complete records and no extractor
contract failures. Preserve all raw exceptions and partial outputs. Only integer
LoopAction counts are bridged to equivalent point-mass PMFs; other representations
are unsupported in this frozen control. Failure-type containment and local-error
substitution preserve all unrelated XML tokens and QName namespace declarations.

All extraction and solving runs remotely with 360-minute job limits and 90-day
artifact retention. Local tests use only small artificial data. Following M9U,
continue all four historical application samples as development and then freeze
a fresh prospective comparison; do not treat this control as task completion.
