# B0-v3 technical binding to three retained applications

Use the unchanged raw-frequency component on existing byte-locked ordinary calibration artifacts: DeathStar and OTel normal technical data from34223543390 (240 attempts/app,80/operation) and Petclinic colocated/NCD ordinary projection from34212928736 (3600 attempts,900/operation). DeathStar/OTel explicitly reuse normal contract qualification as technical calibration, as declared in their original ordinary projection. These are development inputs, not fresh calibration/test campaigns or a new accuracy comparison. Petclinic's earlier probe decoder has no effect on B0, which receives no probe data.

Trusted remote preparation verifies the complete archived artifact identity/size/digest/source head and materializes only requests.json and seal.json. Charge the full source archive transfer separately from the two files the estimator actually reads. The B0 process activates its read boundary before seal loading and counts every calibration attempt, including failures/timeouts. Technical qualification requires all ten operation summaries to have the expected denominators, exact S/N, and a two-file read audit with no blocked read. There is no success-rate threshold or accuracy gate.

Three GitHub Actions jobs, at most10minutes each, pinned Python3.13.15/environment/source hashes. No application rebuild or new traffic. Preserve separate preparation/fit process costs and model bytes/hash; full B0 model artifacts remain remote. Locally retain only compact ten-forecast count/probability summaries, source/read audits, costs and artifact provenance,90-day remote retention including failures. A successful technical binding does not admit main or substitute for independent accuracy/coverage/cost analysis.
