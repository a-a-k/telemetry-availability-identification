# Comparison execution v3: qualified PMX clock-progress correction

Frozen before the first v3 full preflight. Original v1/v2 data, failures, sources and method decisions remain intact. The reason for this version is the confirmed LibReDE zero-step integration defect and its separately qualified minimal repair, not forecast error or a desired accuracy/coverage result.

[Matched qualification](milestones/PMX_CLOCK_PROGRESS_QUALIFICATION_V1_RESULT.md) confirms20 application projections, four controls,48 solver-oracle records,32 unchanged point forecasts, six unchanged unsupported forecasts and two recovered forecasts. Seven source-pinned builds yield one exact derived JAR. The correction preserves original interval endpoints and samples, retains width/20 whenever it advances the clock, and otherwise uses max(1ns,ulp(start)). It neither disables resource-demand estimation nor changes PMX failure estimation or the declared caller-context/integer-PMF bridges.

`configs/v3_comparison_design_v3.json` binds the qualified binary and audit identity. `configs/v3_comparison_execution_v3.json` pins the original/derived JAR hashes, source/class hashes, compiler, qualification proof and complete inherited source locks. The workflow rebuilds that exact binary with Temurin11.0.32+9. Every real invocation checks its qualified SHA. The production launch command,20second startup,1800second watchdog and resource recording are preserved. The extraction AST is unchanged except an explicit launcher argument. The ordinary graph, B0, G0, evaluation, masks, q law, source pools and future-main design are unchanged.

PMX roles record the qualified-build identity. Compact preflight/main auditors verify that every application extraction actually used that binary with the production settings, and that the controls use the same qualified build. They retain all earlier source/role/replay/forecast/evaluator-order checks. A derived diagnostic JAR or120second diagnostic launcher cannot qualify as production. Binary build time and process memory are measured separately once per campaign and shared across its operation/variant fits; they are not silently zero or counted per forecast.

The new full preflight has the original six full-duration application/placement campaigns and20 operation cells per method, but a fresh seed771605 and namespacev3-preflight-771605. Baseline60/calibration900/test900seconds, deadline2seconds, workload/fault seeds, recovery/flush, known controls, ten methods and admission conditions otherwise remain fixed. The unused main seed771601 and main240/800/8000 design are retained. No repaired development forecast is called an independent confirmation. Only a complete new source-CI/preflight/compact audit can authorize the main-source tag and dispatch. Any further experimental correction requires another version and fresh preflight.

The original author's executable and the derived guarded executable remain separately identified in the article and evidence. The claim is a successful scoped computational repair, not a performance-validation claim for LibReDE or a general finding about PMX. No business test outcome was read in the repair qualification.
