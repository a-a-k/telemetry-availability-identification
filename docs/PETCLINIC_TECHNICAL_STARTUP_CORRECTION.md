# Технический пакет: исправление порядка старта MySQL/Java

Вопрос до корректирующего запуска: устраняет ли ожидание SQL readiness установленный bootstrap race для не собранного colocated/NCD условия, сохраняя все параметры измерения и инъекций? Ожидаемый результат — полный 240/3600/3600 attempt census, исходные qualification gates и отдельные learner/evaluator seals. Это одно недостающее условие того же восьмиусловного technical package, main campaigns=0.

Исходный run 34202539028/head 06e83aad5f65f17365c7715ca3b312938b94f1a9, job 101984436169 завершился до baseline. Read-only audit run 34203827517/head a5116086e9bd3da76c80d4a0be42151bc094dc18 проверил actual raw archive 10046679072 (465113 B, SHA256 fdb058f8bfe7020992fbc7195bb8f24748b9526aaf94462e31933e39287f6701), CRC и inventory. [Evidence](evidence/petclinic-startup-audit-34203827517/startup-audit.json), [ошибки с исходными номерами строк](evidence/petclinic-startup-audit-34203827517/service-errors.log).

162 warmup attempts сохранены; calibration/test/baseline ещё не начались. Обе Visits, Customers и Vets завершились exit 1, OOMKilled=false, restart_count=0. JDBC metadata initialization получила Communications link failure / Connection refused. Compose использовал depends_on service_started, без ожидания SQL readiness; повторные HTTP warmup requests не перезапускают завершившийся Java процесс.

Отдельные `petclinic_stochastic_preflight_v2.py`, config и workflow добавляют MySQL healthcheck: TCP connection и SQL SELECT к четырём seed tables. Все пять Java services ждут service_healthy database, collector по-прежнему service_started. MySQL initial temporary server не принимает TCP до завершения init scripts. Runtime после старта, Java properties, image ID, HAProxy HTTP health, четыре contracts, 2 s deadlines, workload и renewal seeds не меняются. Никаких постоянных restart policies нет.

Повторяется только colocated/NCD с seed 771301, 60/900/900 s, 4 RPS и новым namespace v2; v1 failure не удаляется и не считается собранной кампанией. Семь остальных условий v1 завершаются по исходному протоколу и сохраняют свой provenance. Если их qualification gates обнаружат другой дефект, он требует собственного разбора; текущая startup correction не объявляется исправлением ещё не наблюдавшихся ошибок. Не применяется подбор seed или порогов по accuracy/advantage. Полный итог восьми условий составляется после их фактического завершения.
