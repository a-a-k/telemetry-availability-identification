# Ordinary graph observation technical examples, v1

Authority: [correction v3](SIMPAT_CORRECTION_V3.md). Freeze before execution.
Use the exact ordinary artifacts of successful run34223543390, head
e41468f845c8172fcb9a410ea71dd8b7792312c8. No new acquisition or test data.
All application data processing, fitting and saved-model replay run remotely.
Local checks use only artificial dictionaries and compact aggregate reports.

The declared technical events are DeathStar read_user_timeline and OTel
browse_product, one external HTTP call each. Selection follows source contracts
and the native inventory, without prediction/error selection. Both retained
normal windows have 80 attempts per selected event and60 uniform probe ticks.
The two other operations/app stay outside this technical model version. Each
selected trace must have exactly one observed root with the driver-derived
external parent, at the declared entry service, and no other missing parent.
This requirement is deliberately not applied to the multiple-call checkout/cart
events. All observed service-parent edges are retained, irrespective of success
or frequency. Only source-declared synchronous completion is supported here.

Trusted preparation restores exactly the existing six-file ordinary bundle and
enriches only explicit semantic declarations and provenance in its manifest.
Requests, probes and native files must retain identical SHA-256. It does not
estimate, choose an edge set or use response bodies, Docker state, schedules or
test fields. All source bytes, declarations, code, configuration and workflow
are pinned before the enriched role is created.

G is the actual observed service-parent graph plus native DB CLIENT relations
when a unique, compatible declared endpoint resolves a partial native identity.
Any observed system/address/port/database value must match; conflicting or
ambiguous bindings fail. Completed fields and actually observed fields are
reported separately. Such a node is not claimed to be a native DB SERVER.
The earlier generic inventory and its unresolved placeholder remain unchanged.

For OTel the pinned product-catalog source opens PostgreSQL via the declared
connection string. Its compose/env declare astronomy-db, port5432 and astronomy_db.
GetProduct waits for the database query. The required targets are catalog and
its observed CLIENT database peer, with frontend-proxy as entry. The peer stays
in G and the required predicate. Source copies, Git blob/SHA-256 identities and
upstream links are in [semantic sources](evidence/existing-application-semantic-sources/sources.json).

For DeathStar the source reads Redis, conditionally reads MongoDB to fill the
requested range, and waits for ReadPosts from post-storage-service before return.
The required observed services are user-timeline and post-storage, with nginx
as entry. Redis/MongoDB/storage internals lack a qualified native endpoint map
in this input version. Their correct timely behavior is an explicit unmeasured
condition; absence of a span is not evidence that these dependencies do not exist.
This technical graph does not claim full business dependency discovery.

R for the target uses the two deployment-declared replicas bound to ordinary
proxy server names. Native instance identifiers are absent and not fabricated.
All other native and declared DB nodes remain present with deterministic-live
gates, an explicit unmeasured reduction. The target gate is OR of last L4 probe
eligibilities. L4 success is transport reachability, not application readiness.
The existing versioned decoder preserves the completed result of an in-progress
check; unknown categories remain masked.

P is the empirical joint law of the two possibly masked observations, without
replica independence or MCAR imputation. The unchanged exact graph enumerator
computes the I3 sharp model-probability bounds. Phi is ideal static synchronous
reachability of all declared required targets. Routing, fixed state during the
operation, successful unmeasured dependencies and stability of future conditions
are assumptions. No q multiplier, success-conditioned graph filter, learned
physical domain factors, regression or test-based parameter choice is used.
An all-up normal sample may legitimately give probability1; this establishes
neither future reliability nor correctness of the full two-second business event.

Three physical roles execute in separate fresh jobs: trusted enrichment, builder
with exactly six ordinary data files, replay with only model.json. Runtime/code
integrity is checked separately; read guards cover seals, all data loading and
computation. A final collector requires identical model digest, identity and
prediction reports;6/1 reads; zero blocked/evaluator/legacy reads;80 valid roots;
and successful structural controls. Reverse edge order must preserve the full
answer. Removing incoming edges of each required target in turn must force zero
and differ from the positive normal-window model result. A local artificial
control also tests these mutations on a nontrivial7/10 joint law.

The saved model alone must reproduce its prediction. Full ordinary/model
artifacts remain remote. Seven compact source/build/replay/qualification reports,
API/ZIP/member identities and phase wall/user/system/RSS costs are retained in
Git; research artifacts have90-day retention, including failures. Internal stage
timings exclude source preparation unless stated; no speed/scalability claims.

Passing produces technical examples for the two remaining applications, subject
to these semantic and observation restrictions. It does not select G*, prove
business adequacy, support all ten operations, identify a causal mechanism,
justify a refinement, qualify transfer or admit the240 main campaigns. These
remain explicit items in the30-criterion iteration ledger. Main campaigns=0.
