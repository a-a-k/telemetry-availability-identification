# Original AINA source and artifact audit v1

Authority: SIMPAT_CORRECTION_V3.md. This is descriptive reproduction of opened historical data, not an independent experiment or a new causal hypothesis. Freeze before original payload parsing. Source arithmetic already seen: five aggregate rows and original source files. No original graph or live payload has been parsed locally for this audit.

Source run a-a-k/otel-demo-resilience/19590510289, head a8bc5a29a15443ad3a4ef6b88ac710afb97c70c0, completed 2025-11-23. All 250 local nested ZIP byte streams match original GitHub artifact IDs, sizes and SHA-256 digests. Expired original artifact metadata survives. The original outer 100x100.zip is staged as authenticated asset 550517536 in UNPUBLISHED draft release 384736227 in the research repository, 19729586 bytes, SHA-256 eac163231ec07f8faf168a8d701f1fe8952329505a441c42379eccdf9c0cd12a. No release publication is authorized by this workflow. Config locks exact staged source, provenance, workflow, audit and tests.

## Census and arithmetic

Audit all five p values {.1,.3,.5,.7,.9}, all 50 chunks/value, all 100 windows/chunk, all 100 probes/window: 250 archives, 25000 windows, 2500000 probes. Validate outer/inner hashes, ZIP paths, duplicate members, CRC, graph seals, original 5000000 MC sample declarations, complete attempt and endpoint denominators. Retain all member names/size/hashes, compact chunk counts, and errors. Failure is explicit, with partial progress, and never a scientific explanation by itself.

Reproduce historical successful probes exactly: recorded ok and 401/403 skip both counted as successful by the executed collector. Count skips separately. Infer operation from recorded URL path, because failed checkout setup may precede assignment of its POST method label. This is reproduction of the old event, not adoption for the new two-second SLO. Verify per-endpoint reports and realized-mix report arithmetic against raw probe records and original per-endpoint forecasts. Separately recompute the author's five-row global CSV using global MC forecasts; compare fields, without equating uniform expected mixture and realized finite probe weights. Report signed mean error, per-window mean absolute error, forecast async-minus-block difference, and both MAE-difference orientations separately. Do not silently edit source results or article statements.

Count window-log eligible/killed cardinalities and observed killed service names. Check annotations against retained window log records and declared fixed-cardinality K rule. Compare observed eligible cardinality with the model's eligible container pool. These annotations neither prove exact fault timing throughout sequential probes nor reveal the complete runtime eligible universe when containers are never observed killed. Upstream demo was fetched from main, without immutable runtime pin. Do not infer an exact runtime commit from the pipeline source commit.

## Exact semantic controls

Import the byte-locked original resilience.py, load its exact targets and disallowlist, preserve normalization, replica multiplicity, empty-allowlist fallback, Python round/minimum-one K logic, and endpoint_success. Enumerate all labelled container failure subsets for each distinct graph/replica pair, with maximum 16 eligible containers (65536 states). Larger pools are explicitly unsupported; all other audit records remain in the census. Cache by exact graph and replica hashes. For each cardinality, record exact success numerators/denominators and counts of states where async changes each endpoint predicate. Disallowed services remain alive: conclusions concern the original model's allowed failure class. Controls include replicated-service hypergeometric probabilities, an actual async target-removal counterexample and an invariant predicate, plus the original empty-allowlist fallback.

Compare all 2500 retained MC forecasts (250 chunks times two modes times global plus four endpoints) to exact probabilities under their declared source law. Global model selects one of four endpoint predicates uniformly per trial. No new MC draws or fits. Missing original seeds prevent bitwise MC replay. Use the conservative Hoeffding union radius sqrt(log(2*2500/.05)/(2*5000000)) for every forecast, assuming independent Bernoulli trials within each original simulation; no independence across forecasts is required for the union bound. This is an audit diagnostic, not a new superiority test. Invariance is established by exact state comparison, not by nonsignificant MC differences.

## Execution, output and stopping

One GitHub Actions job, Python 3.13.15, maximum25 minutes, contents:read to retrieve authenticated draft asset. No full artifact/model/native downloads locally. Output only compact arithmetic, counts, exact-control summaries without graph topology, byte provenance, errors and measured resource usage; retain90 days including failure. No workload/model rebuilding, no main campaigns, no second deep mechanism or refinement selected by this audit. Any code/interface correction requires a new version, preserved failure/cause and scope, re-freeze and remote recomputation. The original article and original frozen programs remain unchanged.
