# H-EXEC final immutable input audit and replay v1

Original study34230603338/head3477c79c5ca0037979a146346c6b49b6215bac57 completed all32 cells. Its saved analysis rejects full qualification: block4/slot1/repaired reports one malformed native JSON record. Every other check in all32 cells passes; both primary differences are1.0 in all8 blocks, exact p2/256, static control480/480, route diagnostic320/320. Those effects **do not override the failed gate**. Original full records and all34 exact compact ZIPs are preserved.

The acquisition uses a collector file exporter with append:true and flush_interval:1s on a mounted raw-telemetry.log. Compact parsing occurs before the workflow stops the collector and uploads the final raw archive. Its file may still be appended during parsing; the parser reads line-by-line and the compact source hash is calculated later. A transient incomplete final line is a hypothesis, not an established cause: the exact byte stream seen by the live parser was not snapshotted. No gate is waived and no cell is discarded/replaced.

Freeze this audit before reading any original raw archive. Retrieve all32 final raw and original compact artifacts using exact IDs/byte counts/SHA256/source head, plus the original analysis artifact. Materialize only the files needed by the unchanged h_exec_compact_v2.summarize. Reparse/recompute **all32** from final immutable uploaded files. Compare original compact source-member hashes with final native and non-native bytes. All business request outcomes, health, route records, intervention and boundary bytes must match exactly; abort on any non-native difference. Require unchanged bytes before/after replay, preserve every malformed-record offset/length/hash/error without exporting native content locally, and list all changed native counts/qualification checks. No other check may change.

Repeat the original complete gate on those immutable inputs, preserving the zero-malformed/zero-invalid-native requirement and every original threshold. Replay original paired() statistics unchanged; the versioned collection wrapper only separates original acquisition identity from actual replay run/head. Require exact equality of primary block differences, p-values, bootstrap intervals/seeds/draws and every secondary/static/routing diagnostic. The final full gate may pass or fail; save either result. This is a technical source-snapshot audit, not a new sample or a retrospective success threshold adjustment. If malformed data persist, report them and investigate; do not mark confirmation from favorable outcomes.

One remote job, Python3.13.15/pinned requirements,25-minute budget,90-day compact retention including failure. No application reruns, model fits, new independent campaigns, main campaigns or original code edits. Original bytes and provisional failure remain in the record. Full raw/native artifacts stay remote. Before any future H acquisition is reused, place immutable telemetry capture/collector shutdown before parsing so the analyzed bytes equal the archived bytes; the exact future workflow change remains separately versioned.
