# M9Q retained application description

The completed census at `e349db8ef235c3dfc89ba0e9ca440be1b8c017ed`, run
34125799582, contains four results and zero complete projection passes. Both
DeathStarBench samples have no explicitly marked SERVER spans. Both OpenTelemetry
Demo models preserve their selected inventories and six structural/error checks,
but fail the frozen usage-entry comparison. The original decisions remain fixed.

The follow-up reads only the six retained application artifacts on GitHub Actions.
It checks their identities, source run/head, full archive sizes and SHA-256
digests, every learner-contract file, and the generated PCM file hashes. It
exports compact operation counts, expected/actual usage entries, native operation
labels, selection boundaries, timing diagnostics and retained resource usage.
No PMX process, model fit, new acquisition, forecast or evaluator access occurs.

The diagnostic asks whether trace-root counts and earliest recorded execution
counts differ, because the retained `WorkloadService.getStartMessage` selects
the receiving execution of the earliest message, while the application oracle
uses the observed parent graph's root. It also retains the original workload log
lines. Tied timestamps are reported without asserting a reconstruction order.
This describes a possible source of disagreement; it does not revise the gate,
turn a failed application result into a pass, or establish a correct external
request model. All four application samples remain in the report.
