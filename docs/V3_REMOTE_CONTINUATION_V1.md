# Verified remote continuation of v3 comparison

10 September 2026. This is orchestration of the authorized comparison, not a change to the frozen research methods or outcome-dependent selection. All323 v2 repository locks remain unchanged.

## Admission and a single main dispatch

The admission workflow listens only for the terminal preflight34447262633 on the main branch; a manual invocation targets that same fixed run. It verifies canonical repository, source e89e7ca91dd474d8cf2b8c4ed7c1ffc052d98569, original attempt, workflow path and terminal state. The versioned retainer downloads only its28 exact compact artifact names and exact members. The existing frozen compact auditor verifies six campaigns,200 method slots,48 known oracle solves, candidate/input seals, exact replay, permitted reads and freeze-before-closed-outcome access. The workflow and preflight-source CI must both succeed. Error size and superiority never enter admission.

Only then does the coordinator write an admission configuration bound to the protocol, audit, manifest, comparison and execution-lock hashes. It commits the compact evidence and all30criterion snapshot. The admitted source receives the unique tag `v3-main-771601-v2-admitted`, without replacing any existing ref. A new CI at that exact source must succeed before dispatching mode=main. This remains seed771601,240campaigns,800operation cells per method. Tagged dispatch prevents later editorial commits from silently changing the experiment source.

Before every dispatch the coordinator inspects existing runs at that head. It reuses one already-observed run and rejects duplicates. A failed or uncertain dispatch command is never blindly retried. Source CI failure, mismatched hashes, altered tags, unrelated repository runs, repeated attempts or concurrent conflicting pushes stop the automation with an explicit job failure. There is no force push, automatic merge or method repair. A new experimental correction still needs a separately versioned full preflight.

The main dispatch record stores the exact main run, source tag/head, successful source CI and preflight identity. A failed admission run still uploads all compact evidence already collected. Full native, ordinary, model and raw solver payloads remain in their original remote artifacts.

## Terminal main evidence

A second workflow reacts to comparison completion but accepts only the exact persisted admitted main run. It checks the source tag and admission in the actual main commit, retains the964 allowlisted compact artifacts or explicit absences, and verifies the available comparison seal, protocol,240/800/8000 census and mode. An unrelated preflight/comparison completion causes no mutation.

The publication renderer copies remote own-support, per-operation, primary contrast, absence, stable, transfer, stage cost and attempted-census metrics into CSV/Markdown. Null is never zero; missing forecasts cannot acquire metrics. No fitting, replay, resampling or additional inferential test occurs in the renderer. A separate strict main audit checks all240 campaigns,8000 slots,48 oracle solves, sealed candidate/input associations, replay and actual read order. Its pass or failure is retained explicitly. Scientific interpretation remains a distinct obligation. Generated outputs never assert publication readiness. The retained source JSON remains authoritative.

All available main evidence and tables are committed and source CI is checked. Missing analysis, failed jobs or a conflicting push remain explicit failures/limits; they are not promoted into successful method results. The collector produces another all30criterion snapshot without automatically closing unresolved scientific claims.

## Validation

Ten bounded artificial tests exercise admission and dispatch boundaries and lossless reporting of absent results. Both workflow files are checked with actionlint1.7.12. The failed-v1 report remains unchanged; its200compact slots serve only as a formatting/absence regression check. No application acquisition or model execution runs locally.
