# Preserved DeathStar / OTel ordinary input inventory, v1

Frozen before execution; authority: [correction v3](SIMPAT_CORRECTION_V3.md).
This is a technical inventory, not a fitted model or main admission result.

Source: successful run 34205650183, head a5b71467b69dff6ee1d3d6bacc7bace5497654c1.
The configuration pins both original raw ZIPs by API identity, run/head, byte count
and SHA-256. Downloads, native parsing and application inventory run on GitHub
Actions only. Exact source/configuration/runtime helper bytes are locked from the
Git index and verified before each stage. Source archives stay unchanged.

Each source is the 60-second normal-state window with 240 external attempts
(80 per operation), acquired under the complete two-second operation contracts.
These already viewed baseline observations are explicitly reused as technical
calibration: retain all original request IDs, outcomes, times and probe ticks;
adapt only the period label. This is zero new campaigns and zero independent
test evidence. The source contract qualification is selection of a previously
qualified development source, not conditioning the exported rows on success.
Sentinels and their payloads are excluded by exact normal-window request IDs.

Trusted preparation restores the exact source, checks its qualification and
driver trace identity, and maps raw health rows using the unchanged pivot.
The existing v3 ordinary whitelist exports external requests, raw HAProxy
observations, whitelisted native spans and declared deployment metadata. Docker
state, fault schedules, post-test usability, privileged health fields and semantic
response payloads never enter the downstream bundle. Collector malformed rows
are reported; their missingness is not assumed independent. HAProxy L4OK here
means a successful transport connect, not application readiness. In-progress
prefix handling belongs to the later versioned observation decoder.

The declared replica names are bound to raw proxy server labels. Native instance
IDs are retained only when actually present in whitelisted attributes. Missing
native instance identity is not fabricated. Empty source dependency lists mean
unknown at this inventory stage; they cannot justify deleting background
dependencies or define the business predicate. Database peers require complete
ordinary client-span identity under the existing inventory rule.

A fresh consumer job receives exactly five ordinary JSON files plus their seal.
The read boundary is active before verification/loading. It rejects additional
data, network and subprocess access; all six input paths must be read and the
blocked-access list must be empty. Inventory includes actual native service/peer
nodes and parent edges with attempt support, missing traces/parents, native kinds,
available instance identities, raw probe status counts, and roots relative to the
external client context. Multiple root spans can legitimately correspond to the
multiple HTTP calls of one complete operation and are counted rather than merged
or silently discarded. No operation graph is selected by prediction accuracy.

Four compact preparation/inventory reports, API metadata, ZIP/member hashes and
process wall/user/system/RSS costs are retained locally and in Git. Full native
and ordinary bundles remain remote, with 90-day retention including failure
reports. No fitting, PMX invocation, fault injection or test evaluation occurs.
Application model examples remain 1/3 until separately frozen graph construction,
law estimation, saved-model replay and structural/equivalence controls complete
for the remaining applications. Main campaigns remain zero.

Qualification requires both exact source restores, 240 unique external attempts
and three groups of 80, clean native parsing, verified six-file ordinary bundles
and read audits. It does not require low error, a particular discovered graph,
all-success probes, native instance IDs, or one root per complete operation.
Failures preserve reports and are corrected by explicit new versions.
