# Decoder dependency qualification and complete historical rescore

This new protocol follows the failed strict replay gate of run `34218526868` (head `e6f2c11afe9d4b51b466b4d2ba9f66573d8fbfe9`). All 168 builders succeeded and sealed 17,856 old/corrected slots. Strict legacy replay failed in **30** slots; its result/config/candidates remain unchanged. The probability tolerance **1e-6 is not relaxed**.

## Observed replay boundary

All 30 mismatches are DeathStar `trace_only`, B3, colocated→split transfer, compose_post/read_user_timeline, 15 campaign×mode cases. Both historical and replayed values have status `raw_unidentified_optimizer_point`. The largest prediction difference is 0.04157058851480666; maximum absolute likelihood difference is 2.2737367544323206e-13. Multistart transfer ranges are nonzero. The precise platform-level tie-breaking difference is not established; a nonidentified optimizer point is not a reproducible identified estimand.

Within the new run, every field of all old/corrected `trace_only` prediction rows is identical, across all methods/operations/conditions. The same is true for cells with unchanged decoded health files. All slots outside B3/trace_only reproduce probabilities to at most 2.102008200832728e-8; all statuses and absence patterns reproduce. Strict full replay nevertheless remains failed, not retrospectively redefined as successful.

## Dependency argument and known-answer control

`prepare_mode` assigns requests to health ticks by timestamps, which the prefix correction does not change. With `health_policy == 'none'`, `_mask_signals` returns four missing coordinates regardless of decoded values. Therefore observed tick patterns, per-tick outcome counts, baseline residuals, trace topology and calibration outcome arrays are unchanged. `_likelihood_data` reads only these masked patterns/counts and fixed metadata. Route moments read masked ticks; endpoint fallback reads unchanged request outcomes. Raw signals and guarded-transition counts are diagnostic only in these prediction functions. Thus this decoder correction has no prediction-input dependency in the entire trace_only mode. The argument applies to every method/case in that mode, independently of discrepancy sign or magnitude.

The bounded control uses 60 artificial requests/61 health ticks, the actual `prepare_mode` and `_likelihood_data`, and verifies exact equality of all relevant arrays before/after a path-bit change. It also checks a concrete ambiguity of the unchanged historical ND model. With communication factors 1, the source colocated event has probability `g(e_a+e_b−e_a e_b)` and the split transfer event `1−(1−g e_a)(1−g e_b)`. Interior parameter points `(g,e_a,e_b)=(0.9,0.8,4/9)` and `(0.95,0.8,4/19)` both give source probability **0.8**, but transfer probabilities **0.832** and **0.808**. The actual likelihood function gives equal source likelihood within 1e-12. This is a standard identifiability counterexample tied to the code, not a new theorem or a claim about the known scenario parameter in AINA.

## New correction scope, without new fits

The new qualifier checks every candidate member hash, source health digest and changed-tick count against the complete census, all three source seals and all 17,856 identities. It requires full old/corrected prediction-row equality for every unaffected input scope. It then carries forward **the exact original historical row** whenever either (a) decoding of the whole calibration health file is unchanged, or (b) the mode has health_policy none. These uniform dependency rules apply to 7,776 slots: 4,320 trace_only plus 3×1,152 unchanged-file slots in other modes.

For the remaining **10,080 affected-input slots**, require the original strict probability/status/absence replay criterion and use the sealed corrected refit. Any failed replay in this affected scope rejects the new qualification. Keep a per-slot decision and every original 30 mismatch. No raw optimizer value is relabeled identified, no failed historical replay is hidden, no new fitting or independent campaign is added. This implements the author's rule to recompute affected cases and retain unaffected results, rather than forcing an arbitrary point on a nonidentified likelihood surface to match across executions.

## Scoring matrix

Only after this dependency qualification, load preserved M7 evaluator outcomes/health. Recompute all 160 M7 campaigns, all 17,280 prediction slots, and all current/transfer/mode/method/operation cells. Use a 2×2 diagnostic matrix: historical versus accepted corrected predictions, crossed with historical versus corrected test-health window masks. Full all-sequence outcomes remain the same 576,000 attempts. Stable and stable-block-sensitivity views are recomputed from each health version. This separates forecast changes from membership changes in the stability filter.

First reproduce every historical score identity, request/success count and numeric prediction/Brier/signed/absolute/block-bound field to 1e-12 using the exact historical prediction table. Preserve 34,560 complete operation/view census slots per matrix corner, including every absence. Recompute operation and condition summaries, own/common coverage, complete-campaign paired comparisons and the original M7 Brier contrast/summary machinery. Reuse the frozen historical definitions, 10,000 descriptive paired campaign bootstrap draws and seed 771001 from the completed M7 census. Descriptive 95% intervals and original M7 contrast labels remain historical; they are not the six primary v3 G* contrasts. No interpretation as causal improvement or main adequacy follows from this correction.

Keep complete tables, exact hashes, source metadata and four-corner compact results; 90-day retention including failures. One remote job capped at 60 minutes. Input/model fitting, new acquisition, native parsing and PMX execution are absent. Locally only bounded controls and compact aggregate evidence are read. A bounded window control verifies that prefix normalization changes stable membership while preserving every business outcome and the all-sequence census.

After scoring, update the M7 side of the already opened M9X PMX comparison separately and preserve its original version. Other auxiliary historical series require a dependency audit before reuse. Petclinic still has a technical interface qualification, not a previously scored accuracy claim. Primary v3 graph adequacy, supported main class, causal mechanisms and admission remain open.
