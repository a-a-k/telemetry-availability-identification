# Complete computational pipeline and telemetry-volume scaling v1

11 September 2026. The author requests complete pipeline speed ratios, scaling
measurements and a full table in the scientific report. This is a supplemental
computational experiment after the main accuracy results have been opened. It
does not change any original forecast, main contrast or independent campaign n.
The 356 scientific source locks remain unchanged. No manuscript or exports.

## Prespecified workloads and measurements

The source is main34465226083. For each of the three applications use the first
declared colocated/N/repetition0 calibration campaign, irrespective of its
timings and predictive errors. Retrieve only the two exact ordinary/PMX role
artifacts and the original controls by provider ID, SHA256, size and safe ZIP
inventory. Both methods use the same external calibration request population;
their permitted observation types remain different as in the main protocol.

Measure workloads with 1,2,4 copies of the 3600 attempts: 3600,7200,14400 rows.
Copies receive unique request/trace/span IDs and preserve every value, duration,
parent relation, failure, timeout and missing trace. Probe observations are
shared because the copies preserve the same timestamps. This varies record
volume at fixed application structure and empirical state law. It is neither a
longer independent observation period nor a larger deployed application.

Three timing rounds use scale orders1/2/4,2/4/1,4/1/2. Method order alternates
deterministically across scale/round positions. Each paired trial uses the same
runner sequentially, fresh Python/JVM processes and installed dependencies.
Do not claim empty OS caches or three independent application campaigns.
All 27 paired trials are retained, including timeouts, unsupported results,
out-of-memory failures or forecast-invariance violations. Never select a
different campaign because of its benchmark outcome.

## Equal endpoints and explicit accounting

Start: sealed permitted calibration files already on local runner disk and
installed tools. Finish: saved forecast candidates and completed verification.

Graph route includes input loading, all six execution/reachability variants,
G0/B0, serialization and a fresh-process saved-model replay. PMX route includes
native projection, all per-operation extractions and their fixed20s launch
delay, model collection, the complete Maven/JVM analysis process, both variants,
original controls, two solver passes and candidate serialization. All these
sequential stages are inside an outer monotonic wall timer. Record individual
process wall/user/system CPU/maxRSS as well. The graph timing is an upper bound
on the work needed for Gstar alone because the complete family is computed.

Source download, workload copying, software installation/compilation and common
acquisition/role projection precede the timing. Their exclusion defines this as
the complete **computational** pipeline from prepared telemetry, not elapsed
time to observe a live system or a GitHub job including queues. One-time PMX
compiler/analyzer setup is retained separately. Fixed launcher delay remains
inside the primary observed pipeline; an arithmetic view subtracting that
declared delay must be labelled as such, never a measured optimized runtime.

The pipeline peak-memory comparison is the largest GNU-time process/children
high-water value among its sequential stages, not a sum of peaks. CPU time may
be summed across these disjoint timed child commands, but not added again to
nested native/solver timers. Parent orchestration overhead is captured in the
outer wall timer and is not falsely called measured total RSS.

## Qualification, summaries and scope

Each repeated/scaled forecast must retain the original status and probability
or both identified endpoints to1e-12; sample counts increase but independent n
does not. Known-probability PMX controls and exact graph replay must pass. A
failure yields an explicit row and prevents treating that trial as a speedup.

For each application/volume report requests, bytes, spans, graph/model size,
all three raw paired measurements, median/min/max wall, summed child CPU,
process maximum RSS, PMX/graph ratio per pair and its median/min/max. Report
T(2N)/T(N),T(4N)/T(N), throughput and log-ratio scaling exponents descriptively.
There is no new significance family or claim of an asymptotic law from three
volumes and three technical repetitions. Include startup/setup and both method
supports beside the ratios; report all planned workloads even when incomplete.

The deterministic transformation is covered by artificial identity/edge/time/
missingness controls. Actual native/model extraction and benchmarking run only
in GitHub Actions. Locally retain only exact allowlisted compact results and
provenance; full computational outputs remain remote.
