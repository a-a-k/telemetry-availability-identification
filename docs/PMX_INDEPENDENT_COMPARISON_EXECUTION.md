# Completing the independent PMX availability comparison

Active task, 8 September 2026: the user explicitly requested completing this
comparison for the article. The earlier bounded M9R result is historical,
not a reason to leave the comparison unfinished. Preserve earlier evidence and
proceed through a scientifically interpretable comparison, without substituting
a control-model success for application predictive validation.

## Completed prerequisites

- M9S qualifies the failure-type containment plus equivalent integer-loop PMF
  representation on the pinned solver. All 16 positive controls pass and all
  eight prior negative records reproduce.
- M9T provides the complete learner-only request/operation census for the four
  historical M7 native samples. Compound OTel user operations span multiple
  HTTP roots; DeathStar semantic timeouts need not have a native span error.

## Next implementation and validation

1. Define and test an external-request adapter. Use calibration requests only
   for application parameterization. Preserve each user-operation identity and
   its recorded trace ID; add a disclosed request wrapper for multiple HTTP
   roots and no-trace cases. Keep original native errors, timestamps, parent
   relationships and provenance, with explicit records of adapter additions.
   The target is the full external request, with whole-traffic scoring primary
   and the frozen stable-subset view separately descriptive.
2. Test both observation-boundary rules against artificial cases before application:
   explicit SERVER operations for OTel; a source-grounded service-boundary view
   for DeathStar, whose spans lack that marker. Never call inferred roles native
   instrumentation. Cases must cover forests, an absent trace with a timed-out
   request, repeated calls, colliding operation names and propagated errors.
3. Let the pinned PMX binary extract structure and its own operation frequencies.
   Preserve the native-inclusive parameterization with the qualified serialization
   bridge. Define an independently estimated conditional-local sensitivity from
   calibration joint error counts, retaining its propagation/independence
   assumptions and any unsupported operation. Both need the same declared
   external outcome and request denominator. Do not import the proposed method's
   fitted probabilities or graph as the independent extractor result.
4. Apply the qualified contract to all four historical samples and obtain the
   development comparison: proposed fixed estimator, B2, matched endpoint and
   PMX variants. Keep all six user operations, both placements, failures and
   coverage. Record acquisition, adaptation, extraction, estimation and solving
   costs separately. Any model change informed by this comparison remains
   development and requires fresh validation.
5. Freeze the compared implementations, matrices, seed namespaces, primary
   contrasts, campaign weighting, missing-prediction handling and precision
   planning before fresh acquisition. Both applications, placements and N/NC/ND/NCD
   laws belong in the comparison's coverage census. Derive or explicitly bound
   the confirmation budget; do not copy M7 or M9P sample size as proof of adequate
   precision for a different contrast. Do not promise a noninferiority or
   superiority claim without its prospectively justified criterion.
6. Run the fresh learner/candidate/evaluator sequence and artifact audit through
   completion. Write the comparative report and update the article's claim
   ledger from the measured outcomes, including unfavorable comparisons.

All full native processing, fitting, extraction and solver execution run on
GitHub Actions with 360-minute job limits. Local work is source development,
bounded artificial tests and inspection of generated aggregates. Already viewed
M7/M9P results are not untouched confirmation of a revised PMX method. Remaining
limitations must be reported precisely; they do not by themselves establish
predictive superiority or lower end-to-end cost for the proposed method.
