# Application execution binding v2: declared probe layer and Valkey completion

This correction preserves the complete [v1 result](milestones/V3_APPLICATION_EXECUTION_V1_RESULT.md), code/config and compact artifacts. The finite execution core and its S3/I5 statements do not change. V2 is a new application adapter/protocol, frozen before its remote replay of the same previously opened calibration data. There is no new application traffic, independent N or main-series result.

## Exact changes

`declared_probe_layer` accepts exactly the ordinary declaration ['L4OK'] or ['L7OK'] and passes that layer to the existing corrected last-check decoder. Unsupported declarations fail explicitly. Thus DeathStar/OTel retain L4 and Petclinic uses L7. Matching uses the same request-start alignment and two-second maximum observation age; no additional truth or state fields are introduced. L7 observations are still not treated as timeless physical capability or proof of full business completion.

The native cart Redis CLIENT relation is completed by the explicit peer `{owner:cart, system:redis, address:valkey-cart, port:6379, database:0}`. Provenance: pinned OTel `.env`/compose select VALKEY_ADDR=valkey-cart:6379; ValkeyCartStore constructs options without a database override and calls GetDatabase(); its pinned dependency is StackExchange.Redis3.1.31. That tag resolves to commit `90bbf1fcba1c9cc464c94b96d16c2706f590383f`; GetDatabase defaults its argument to -1 and ApplyDefaultDatabase uses the nullable configured default or the integer default0. Exact source files, licenses, bytes, Git blobs and SHA256 values are in [v3-valkey-source-binding](evidence/v3-valkey-source-binding/sources.json). The [official client documentation](https://seredis.dev/Configuration.html) distinguishes the nullable DefaultDatabase configuration; the numeric0 conclusion here follows the pinned source, not an assumption based on a current documentation default. Missing native namespace is still explicitly recorded as completed from declaration, not relabelled as an observed attribute. Any conflicting/nonunique peer remains unsupported.

Source-declared optional RPC methods now propagate optional status to descendants by actual parent links. In particular, checkout's ignored EmptyCart error cannot make its nested DB operations or target-replica demands mandatory. All spans, DB nodes and graph edges remain in discovery. Only DB relations owned by required services/entry and observed in mandatory contexts enter the required completion set. Cyclic parent chains are rejected. The ignored-method rule is tied to its declared cross-service edge and method name, not a generic suppression of errors.

These are observation/binding repairs within the existing H-EXEC refinement class, not a new independently confirmed mechanism or a result-driven change of the business contract. The new Valkey relation closes previously unsupported OTel operations. Petclinic's two target-dependent models must be recalculated. Three DeathStar operations, OTel browse_product and Petclinic list_owners/list_vets must reproduce their v1 full calculation hashes exactly; those six hashes are frozen in the new config and checked by the builder.

## Controls and finite remote package

Five bounded controls pass: L7 success reproduces the v1 erroneous ambiguity then resolves under the declaration; L4 calculations are exactly unchanged and business-label flips have no effect; an unknown probe contract never falls back; a unique Valkey declaration completes only the missing namespace and competing peers are rejected; an ignored RPC's failing DB descendant/other replica remain optional while the graph persists. The 14 earlier core/binding controls remain frozen with their original scopes.

The same nine jobs, ten-minute caps, three-way stage concurrency, ordinary6/model2 physical read roles, model seal/hash replay, structural controls and90-day retention from v1 apply. All ten operations must have explicit supported/ambiguous/unsupported status; unsupported is not replaced by zero. Ordinary and full-model payloads stay remote; only exact allowlisted compact reports/metadata are retained locally. The v2 config locks all source/code/protocol dependencies against exact Git index bytes. Main admission and independently measured accuracy remain outstanding.
