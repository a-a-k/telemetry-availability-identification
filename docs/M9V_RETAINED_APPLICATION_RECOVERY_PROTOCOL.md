# M9V retained-model recovery: exact IDs and mixed loop representations

The first M9V run 34191216681 retains all twelve successful operation/error/edge
PCM checks, but no accepted solver input. The six DeathStar cases fail an audit
regex requiring 32-character trace IDs; the native IDs have 16 characters. The
six OTel cases pass the original reconstruction audit but encounter native PMX
IntPMF loop distributions rejected by the integer-only M9U bridge. Six OTel
conditional-local variants are independently unsupported by observed parent
success with a child error. Those semantic abstentions remain unchanged.

The read-only diagnostic run 34192089947 reproduces every original rejection and
resolves all twelve exact trace/span/operation/parent/time inventories by checking
printed hexadecimal identities against the supplied envelope. Every trace is
reconstructed once; no native timestamp, identifier or parent changes. It records
every original loop expression, including zero/one and one/two invocation mixtures.

This recovery reuses the exact retained PMX outputs, projections and comparator
fits. It makes no new PMX call, fit, acquisition or evaluator read in preparation.
All source ZIP bytes/digests and file contracts are verified remotely. The new
bridge converts nonnegative integer LoopAction literals to equivalent IntPMFs
while preserving already valid IntPMF expressions byte for byte. A strict parser
checks integer support, unique outcomes, finite nonnegative probabilities and
mass one within 1e-12. Unsupported expressions are rejected, not approximated.
The previous failure-type and conditional-local substitutions remain unchanged.

Two accepted M9S random-loop controls are retained separately. Their two leaf
point masses are supplied to the new bridge as integer literals; their random
root law is printed as `IntPMF[(0;5.0E-1) (2;0.5)]`. The final models differ from
accepted M9S only in equivalent whitespace/scientific probability notation. Both
must return .724 within 1e-12 in both passes, with valid physical probability mass.

All 18 supported application variants and these two artificial controls are
solved twice by the unchanged M9S harness and pinned analyzer. The four artificial
oracle passes gate candidate publication. Every application result must have
valid normalized probability mass and agree across the two passes. All twelve
native-inclusive forecasts and the six supported DeathStar conditional-local
forecasts are attempted; the six unsupported OTel variants remain explicit.

The adopted historical input contract records its original hash, source run and
commit before the new execution metadata are sealed. Its projection and comparator
file hashes remain unchanged. Candidate publication and subsequent evaluation
reuse the M9V scoring implementation and whole-traffic primary view. The original
M9V failures and its baseline results remain historical evidence. This is still
development on previously inspected data, never fresh confirmation or a new fit.

All processing and solving run on GitHub Actions with 360-minute job limits and
90-day retention. Preserve the unused initial M9V analyzer-build cost as well as
this recovery's preparation and solving cost. Continue to prospective confirmation
after obtaining interpretable development forecasts; do not stop at this repair.
