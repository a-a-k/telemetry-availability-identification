# Prospective comparison v2: explicit request-profile bridge

Frozen correction prepared on10 September2026 after the [v1 full-duration preflight failure](milestones/V3_COMPARISON_PREFLIGHT_V1_RESULT.md). The [author's v3](SIMPAT_CORRECTION_V3.md) and [final method definitions](V3_FINAL_COMPARISON_BINDINGS_V1.md) remain authoritative. Main is still0/240.

## Observed defect and exact change

In run34442870952 at28eb21c78b2ade594fd493d0fc656fa2fa6dda79, all six application PMX projections fail before extraction with `KeyError: 'profile'`. The sealed native/request role has exactly eight request fields; the original PMX adapter expects an application name for its synthetic request wrapper. The earlier v1 fixture tests used richer legacy rows, leaving this real schema transition untested.

`v3_comparison_pmx_v2.py` is a separate version of the prospective PMX bridge. It first checks that each incoming row has exactly `REQUEST_FIELDS` and that the application has a declared policy. For the legacy adapter invocation only, it copies each row and adds `profile=identity.application`, where that application has already been checked against the sealed role/manifest and frozen execution configuration. The physical input files and caller's in-memory rows remain unchanged. The read audit records this metadata origin. An extra profile or closed-outcome field in the incoming row is rejected. No source q, routing law, outcome, parameter, native span or test observation is fabricated.

The same DeathStarBench service-boundary, OpenTelemetry explicit-SERVER and Petclinic SERVER/MySQL-CLIENT policies remain selected. PMX conditional-local remains primary, inclusive remains additional, and the original independent context/database controls, author binary/options and Palladio route are reused. This is metadata compatibility, not a claim that v1 application predictions existed or were numerically preserved: v1 produced none. On artificial exact-schema records, v2 equals the qualified legacy projection with the same profile supplied explicitly.

## Frozen execution and data roles

The new workflow `.github/workflows/v3-prospective-comparison-v2.yml` is generated by `scripts/build_v3_comparison_workflow_v2.py`. It invokes the frozen v1 acquisition, graph construction, role sealing, replay, evaluation and analysis with the new explicit design/configuration paths, and invokes the new PMX bridge. The existing `v3_main_admission_v1.json` schema remains the separate main gate; that file is not created here and its protocol digest must match the v2 design.

`configs/v3_comparison_design_v2.json` has SHA256 **7a627ed1241a3247b558fcb3ede26279f800f566ca2a2720d85f90fb2bbf6aef**. The new six-case full-duration preflight uses seed771604 and namespace`v3-preflight-771604`. The main seed771601, main namespace,240cases,800operationcells/method, ten methods,60/900/900s durations,4attempts/s, failure-law parameters and six-contrast analysis remain unchanged. This version is frozen before its new acquisition; all v1 artifacts and outcomes retain their original development status.

`configs/v3_comparison_execution_v2.json` retains the312 original file locks and adds the old execution configuration plus the v2 implementation/design/workflow/tests/correction/retention/audit files. No original locked file is changed. A scoped `configs/.gitattributes` entry pins LF for the two new JSON files so Windows checkout preserves their protocol bytes; it does not alter attributes for the original frozen configurations. The v2 retainer/auditor selects the new exact workflow/design identity and applies the same strict compact-only retention and actual-evidence admission checks, including the PMX bridge version.

## Controls and admission limits

Four new bounded PMX tests cover all three policy families on the exact eight-field schema, reproduce the v1 error, compare complete projection objects against the explicitly supplied legacy metadata, reject spoofed/extra/test-period data, preserve every untraced attempt, and exercise the four-file physical read boundary using artificial data. Three version-matched compact-audit tests cover a complete artificial six-campaign/48-oracle chain and tampered read, replay, oracle, ordering, run and member evidence. These local controls do not execute a real PMX binary or process application telemetry. Actionlint verifies the generated workflow.

The entire v2 acquisition-to-PMX-to-freeze-to-evaluation chain must pass a new remote full-duration preflight. Neither local tests nor the preceding graph/evaluator/control successes admit main. Low error and superiority are not gates. The observed seven ambiguous Gstar cells and three unsupported native-parent cases from v1 remain in the record; the graph method, its bounds and its strict structural scope are unchanged. No midpoint, inclusive substitution or numerical tolerance is introduced to create a point forecast.
