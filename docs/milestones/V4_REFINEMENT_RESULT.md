# Результаты доработки после рецензии: независимая проверка, точность, затраты и пропуски

Согласованный вклад — **формализация стохастической модели успешности операций, аналитическое исследование её свойств и границ применимости, автоматическое построение и идентификация по телеметрии**. Используются совместные частоты и точные вероятностные вычисления, без ML. Доработка проверяет соответствие формального события реальной операции и стоимость его вычисления. Превосходство над частотной B0 не является условием научного вклада.

Исправленная привязка прошла новую независимую проверку в объявленных условиях. Все 64 800 внешних исходов проверены по первичным свидетельствам. На 61 200 поддержанных попытках нет несовместимости с модельными границами в обоих направлениях; 4411 из них остаются неоднозначными. Прогнозные ошибки ниже, чем у топологической G0 на общей поддержке; превосходство над PMX/B0 не установлено. При первом построении и расчёте подготовленный алгоритм быстрее всех шести проверенных конфигураций CUDD/aGrUM/Storm на каждой из 59 новых calibration-моделей, с одинаковыми ответами. Сохранённый менеджер CUDD даёт преимущество при проверенном структурном изменении. Отдельное новое сравнение исправленного конвейера восстановления с PMX/Palladio завершилось без отказов во всех 27 парах: медианные коэффициенты 31,73–100,24× для границы от подготовленной телеметрии до первого сохранённого прогноза. При больших пропусках корректные границы могут оказаться малоинформативными.

## Что исправлено и почему

Первый требуемый результат — [таблица первичных свидетельств, причин, предположений, исправлений и распространённости](V3_ATTEMPT_AUDIT_V1_RESULT.md). Полный исторический аудит охватывает 240 кампаний: старая привязка исключала 2541 зарегистрированный успех на 817 200 поддержанных попытках. Отдельный первичный аудит воспроизвёл 39 831 из фиксированных 43 200 внешних исходов; для 3369 старых Petclinic write-ack не хватало исходных SQL-строк. Эти ограничения сохранены.

Корректная запись прошлого отрицательного proxy verdict не доказывает невозможность принять вызов позднее, после восстановления. Привязка X теперь использует свидетельства приёма обязательного вызова в окне операции; отсутствие такого свидетельства даёт неизвестность. Старые proxy-значения сохраняются совместно как вспомогательные наблюдения. Второе исправление касается объявленного однократного повтора Petclinic: неудача первой транспортной попытки не исключает завершение логического обязательного вызова, если установлен допустимый повтор 503→201 с необходимыми связями и порядком. Недостаточные свидетельства сохраняют неизвестность.

Это исправления интерпретации наблюдений, а не стирание неудобных исходов. Внешний бизнес-исход не поступает в построение состояния. [Спецификация привязки и предпосылки](V4_BINDING_REVISION_V1.md) и [ретроспективная диагностика 29 кампаний](V4_BINDING_DIAGNOSTIC_V1_RESULT.md) предшествуют независимому подтверждению. Историческое снижение 535→0 само по себе не объявлялось подтверждающим результатом.

Форма события `E(Z,C)=g(Z) ∧ AND(C)` сохраняется, но C обозначает обязательные логические завершения в оговорённой семантике. Сокращение через `K=AND(C)` и эквивалентность вычислительных представлений сохраняются для объявленного семейства совместных законов на декартовых масках наблюдения. Достижимость концов относится к этому формальному семейству; физическая реализуемость любого формального свидетельства не утверждается. При добавлении глобальных ограничений на допустимые состояния предпосылки следует проверить заново. Само сокращение завершений не доказывает превосходства над BDD, которая также эффективно представляет конъюнкцию.

## Независимая серия и барьеры

До открытия новых проверочных исходов зафиксированы [протокол](V4_CONFIRMATION_V1_PROTOCOL.md), объём и критерии: три приложения, размещение split, законы N/NC, три повтора — 18 кампаний и 60 позиций «кампания × операция». Получены 64 800 calibration- и 64 800 test-попыток. Ещё 48 normal/retry-контролей анализируются отдельно.

Все научные стадии запуска **34703686592**, source `12ddb09369ee6e0b60ca4c7db6fe1e749530b7df`, завершились успешно. Все 18 численных наборов прогнозов, включая PMX и B0, запечатаны прежде первого открытия test-исходов. Независимая сверка времени заданий дала:

| Барьер, 2026-09-12 UTC | Завершение всех предшествующих заданий | Начало первого следующего задания |
| --- | --- | --- |
| Прогнозы → расчёт состояний test без Y | 16:49:12 | 16:49:14 |
| Границы состояний test → проверка внешних исходов | 16:50:19 | 16:50:21 |

Все восемь [заранее заданных содержательных критериев](../tables/v4-confirmation-v2-34703686592/scientific-checks.json) выполнены. Исходный общий статус workflow остаётся `failure`: после успешных научных стадий старая таблица переноса запросила отсутствующее colocated-размещение. [Исправление сводки](V4_CONFIRMATION_SUMMARY_V3_REPAIR.md) отмечает перенос как `not_planned` и не меняет ни одного измерения. Отдельный агрегатор **34707482231**, source `a19fef0b998eb1f995ebd06acdde10acd819ff19`, хранит собственную идентичность наряду с источником измерений. Численные прогнозы или модели после исходов не перестраивались. Исторический C12 первоначального исследования остаётся документированным отклонением; новая серия его не переименовывает.

Первый сбор **34701084950** остановился до открытия исходов из-за технической ошибки шаблона retry-контроля. [Поправка v2](V4_CONFIRMATION_V2_TECHNICAL_AMENDMENT.md) сохранила байт в байт все 12 завершённых кампаний и повторила только шесть технически потерянных Petclinic. Полный [источник каждой кампании](../tables/v4-confirmation-v2-34703686592/acquisition-source-map.csv) сохранён. Изменения состава по результату или времени вычисления не было.

## Адекватность события и прогнозная точность

| Приложение | Проверено внешних test-исходов | Поддержано попыток | Неоднозначно | Структурный отказ, попыток | Несовместимо с Y |
| --- | ---: | ---: | ---: | ---: | ---: |
| DeathStarBench | 21 600 | 18 000 | 1382 | 3600 | 0 |
| OpenTelemetry | 21 600 | 21 600 | 3029 | 0 | 0 |
| Petclinic | 21 600 | 21 600 | 0 | 0 | 0 |
| Всего | 64 800 | 61 200 | 4411 | 3600 | 0 |

Первичная проверка HTTP/SQL/контрольных свидетельств не нашла расхождений с зарегистрированными метками. Для каждого поддержанного частичного состояния проверены обе стороны `lower_i ≤ Y_i ≤ upper_i`. На 22 278 полностью наблюдённых состояниях дополнительно проверено непосредственно E=Y. Всего 56 789 поддержанных попыток имеют однозначный результат события; полная наблюдаемость всех координат для этого не обязательна. Проверено 1960 концов свидетельств в основной test-серии.

Все 48 дополнительных контролей независимо проверены и дали однозначный успех: 24 обычных и 24 с принудительным первым 503 и успешным повтором 201. Все 24 retry-группы установлены по native-свидетельствам. В обычной Petclinic-серии дополнительно встретились две такие разрешённые группы. Контроли не добавлены к прогнозной выборке.

Это подтверждает адекватность исправленной семантики в исследованных условиях. Ноль несовместимостей не означает нулевую ошибку вероятностного прогноза: неопределённое состояние допускает оба исхода, а прогноз по calibration может отличаться от частоты в test. Попытки внутри кампании также не объявляются независимыми Bernoulli-повторами для универсальной гарантии.

Прогнозы ниже построены по calibration и зафиксированы до test. MAE — средняя абсолютная разница между вероятностью прогноза и наблюдаемой test-частотой, в процентных пунктах, с объявленным усреднением по условиям и операциям. Для интервала середина не подставляется вместо точечного прогноза.

| Приложение | Gstar: точки / интервалы / отказ из плана | MAE Gstar на точках, п.п. | Gstar−G0 на общей поддержке, п.п. | Gstar−PMX на общей поддержке, п.п. |
| --- | --- | ---: | ---: | ---: |
| DeathStarBench | 18 / 0 / 0 из 18 | 0,9444 | −5,0093 | ≈0 |
| OpenTelemetry | 12 / 5 / 1 из 18 | 2,0000 | −13,6574 | −0,0817 |
| Petclinic | 24 / 0 / 0 из 24 | 0,9259 | −2,1991 | +0,00395 |

Calibration-моделей 59, из них 54 точечные и пять интервальных; test-состояния поддержаны в 57 из 60 случаев. Это разные входные совокупности: единственный calibration-отказ приходится на OpenTelemetry, три test-отказа — на DeathStarBench. Поддержки не отождествляются и отсутствия не исключаются из учёта.

Заданные парные интервалы разницы MAE относительно G0: DeathStarBench `[−5,7731; −3,9074]`, Petclinic `[−2,4306; −1,9769]` п.п. Для OpenTelemetry общая поддержка составляет 12 позиций в четырёх полных кампаниях; по фиксированным правилам это только точечная описательная оценка без интервала. Для PMX интервалы в OpenTelemetry `[−0,4190; +0,4669]` и Petclinic `[−0,003264; +0,009020]` включают ноль. Область этих bootstrap-результатов условна на показанной общей поддержке; отсутствие различия не является доказательством статистической эквивалентности.

**На всех 54 общих точечных позициях прогнозы Gstar и B0 совпадают точно как рациональные числа**: 18/18, 12/12, 24/24. [Сверка готовых прогнозов с их SHA](../tables/v4-confirmation-v2-34703686592/gstar-b0-common-points.csv) не исполняет модель заново. Поэтому собственную MAE B0 2,1852 п.п. на всех 18 OTel-позициях нельзя сравнивать с MAE Gstar 2,0000 на 12 позициях как преимущество метода. На этих же 12 позициях ошибки равны. Содержательная ценность формализации — структура события, условия применимости, вывод из native-наблюдений и явная неопределённость, а не улучшение уже непосредственно наблюдаемой частоты за счёт другой записи того же события.

PMX/B0 дополнительно [разобраны по 40 настоящим сохранённым PCM](V4_PMX_B0_EXPLANATION_V1_RESULT.md): независимый пересчёт воспроизвёл все ответы Palladio с максимальным отклонением 1,11×10⁻¹⁶. PMX использует calibration-внешние исходы, как и B0; отличия определяются корневым множителем и композицией смоделированных вызовов. Это не независимое восстановление произвольных зависимостей одной заменой решателя.

Полные [600 слотов прогнозов, поддержки, контрасты и ошибки](../tables/v4-confirmation-v2-34703686592/forecasts/README.md), [60 строк первичной проверки](../tables/v4-confirmation-v2-34703686592/primary-compatibility.csv) и [отдельные контроли](../tables/v4-confirmation-v2-34703686592/directed-controls.csv) доступны без выборочного отбора удачных случаев.

## Производительность исправленного вычисления

Первое измерение **34707544825**, source `a19fef0b998eb1f995ebd06acdde10acd819ff19`, включает все 59 поддержанных calibration-моделей из 60 плановых позиций. Представлены обе реализации нашего вычисления, fixed/sift CUDD, conditioned/joint aGrUM, sparse/compact Storm; исходный и общий сокращённый вход; семь режимов; три технических повтора. **Все 19 824 расчёта прошли проверку по 11 парам рациональных границ и статусам; отказов, таймаутов и замен не было.** Это согласие точного ответа по одной заданной модели, отдельно от прогнозной точности.

Первое построение включает преобразование, проверку ключа, создание вычислительного представления и первый запрос. Импорт библиотеки вынесен из этого времени у всех участников и включён в отдельное время/RSS процесса. Дополнительная верификация выполняется после всех измерительных процессов профиля. Один backend сохраняет доступную инфраструктуру между всеми семью запросами; критерий необходимости перестроения включён в таймер. Общая проекция `K=AND(C)` доступна всем участникам с учётом её затрат. Параметры и варианты зафиксированы до измерения, выбранный вариант не меняется под каждый удачный случай.

В таблице **K = время конкурента / время нашего подготовленного алгоритма**. Сначала взята медиана парных отношений трёх повторов для каждой модели, затем медиана по моделям приложения. Это не отношение двух непарных медиан. K>1 означает выигрыш нашего подготовленного вычисления. Для каждого столбца сохранены 18/17/24 полные модельные пары соответственно.

| Вход | Конкурент, первое построение + расчёт | DeathStarBench K | OpenTelemetry K | Petclinic K |
| --- | --- | ---: | ---: | ---: |
| Исходный | CUDD fixed | 1,992 | 2,084 | 4,503 |
| Исходный | CUDD sift | 2,272 | 2,192 | 4,212 |
| Исходный | aGrUM conditioned | 8,283 | 9,843 | 8,180 |
| Исходный | aGrUM joint | 6,562 | 6,183 | 7,139 |
| Исходный | Storm sparse | 2,975 | 3,447 | 2,962 |
| Исходный | Storm compact | 2,664 | 2,540 | 2,775 |
| Общее сокращение | CUDD fixed | 2,194 | 1,844 | 3,681 |
| Общее сокращение | CUDD sift | 2,191 | 2,028 | 3,909 |
| Общее сокращение | aGrUM conditioned | 6,791 | 7,580 | 6,425 |
| Общее сокращение | aGrUM joint | 5,289 | 4,657 | 5,514 |
| Общее сокращение | Storm sparse | 2,541 | 2,151 | 2,661 |
| Общее сокращение | Storm compact | 2,396 | 1,967 | 2,515 |

Выигрыш первого построения есть на каждой из 59 моделей относительно каждого конкурента в обоих представлениях, по медиане трёх парных повторов. Абсолютные медианы времени подготовленного алгоритма на исходном входе — **1,781 / 1,547 / 1,067 мс**; исходного алгоритма — **1,346 / 2,125 / 0,838 мс**. Подготовка ускоряет дальнейшие запросы, но на небольшом однократном запросе может стоить дороже прямого расчёта. Это две реализации вычисления одной модели, а не две разные вероятностные модели.

| Режим, исходный вход | Конкурент | DeathStarBench K | OpenTelemetry K | Petclinic K |
| --- | --- | ---: | ---: | ---: |
| Повтор того же запроса | CUDD fixed | 1,089 | 1,191 | 1,143 |
| Повтор того же запроса | Storm compact | 1,722 | 1,042 | 1,503 |
| Добавление структурной связи, manager сохранён | CUDD fixed | **0,406** | **0,429** | **0,537** |
| Добавление структурной связи, manager сохранён | CUDD sift | **0,548** | **0,589** | **0,795** |

При проверенном структурном изменении CUDD fixed быстрее подготовленного алгоритма примерно в **2,46 / 2,33 / 1,86 раза**. На общем сокращённом входе преимущество CUDD также сохраняется. Повторные запросы дают гораздо меньший разрыв, чем первое построение; отдельные модельные пары могут благоприятствовать конкуренту. Например, для OTel непарные медианы повторного запроса Storm compact / ours prepared равны 0,464 / 0,518 мс, тогда как медиана парных отношений равна 1,042. Эти статистики имеют разный порядок агрегирования; выбирать удобную из них как всеобщую победу нельзя.

Медианный пиковый RSS отдельного процесса на исходном входе: подготовленный алгоритм **34,4–36,1 MiB**, CUDD **59,6–61,4 MiB**, aGrUM **46,4–47,9 MiB**, Storm **93,3–94,8 MiB**. Это процесс с импортированной библиотекой и семью запросами, не чистый размер графа и не память всего приложения. Размеры представлений и счётчики перестроения сохранены отдельно.

**Полная таблица всех режимов находится в [вычислительном отчёте](../tables/v4-confirmed-performance-v2-34707544825/REPORT.md)**: 336 строк сводки стадий, 19 824 исходных измерения, 2832 записи ресурсов процессов, 11 564 парных отношения, размеры представлений на каждом снимке и отдельный пустой census вычислительных отказов. Включены первое построение, повтор, новые частоты, новые маски, изменение метаданных, структуры и возврат исходной структуры. Все конфигурации и оба наших алгоритма сохранены, включая менее выгодные результаты.

aGrUM решает эквивалентную задачу через специальное сведение на вспомогательный полный носитель и рациональное агрегирование частот; это не опыт с произвольной credal network. Storm проверен в двух согласованных DTMC/MDP-кодированиях. Вывод относится к этим реализациям. Новая серия не повторяет прежние 27 синтетических позиций; [предыдущее симметричное сравнение и его масштабирование](V3_EXACT_COMPARISON_V2_RESULT.md) сохраняют отдельную область вывода, включая режимы, благоприятные для CUDD. Аналитического универсального превосходства над символическим алгоритмом не заявляется.

## Затраты автоматического восстановления и полного процесса

[Полная выгрузка новых затрат стадий](../tables/v4-confirmation-v2-34703686592/recorded-pipeline-costs/README.md) содержит извлечение, идентификацию, расчёт, процессы/CPU/RSS, входные объёмы, каждую загрузку и решение PMX-модели. Зафиксированные медианы дают следующий масштаб, но строки имеют разные единицы наблюдения и не делятся друг на друга для получения общего ускорения:

| Стадия | Наблюдений | Медиана, с | Единица и граница |
| --- | ---: | ---: | --- |
| Разбор общего native-пула | 18 | 3,34846 | Кампания |
| Извлечение графовой модели | 59 | 0,203327 | Поддержанная операция |
| Идентификация графового семейства | 59 | 0,00309346 | Поддержанная операция, совместно для вариантов |
| Расчёт графового семейства | 59 | 0,00119560 | Поддержанная операция |
| Проекция native-входа PMX | 60 | 2,34195 | Операция |
| Независимый процесс извлечения PMX | 60 | 23,4318 | Операция, включая объявленный запускатель |
| Сбор/экспорт общей телеметрии | 18 | 2096,92 | Кампания, общая инструментированная система |

Для перенесённых 12 кампаний стоимость сбора относится к первоначальному сбору. Время внешнего процесса и вложенной стадии пересекается; общие solver-batches, мониторинг приложения и время вычисления одного ответа имеют разные границы. Незамеры не заменены нулём.

**Новый парный запуск 34708991534 завершён: все 27 из 27 пар прошли проверку, отказов и таймаутов нет.** Измерительная версия — `4d5270008e6845bc55220869da721698be347e68`, [протокол зафиксирован до измерения](../V4_PIPELINE_BENCHMARK_V1.md). Взята первая объявленная calibration-кампания split/N/0 каждого приложения; 3600, 7200 и 14 400 попыток получены детерминированным копированием входов с сохранением структуры и эмпирического закона. Три повтора технические: независимая научная выборка от копирования не увеличивается.

Граница измерения — **«подготовленная телеметрия и установленные инструменты → первый сохранённый прогноз»**. Наш процесс строит исправленные модели и рассчитывает все шесть графовых вариантов плюс G0/B0. PMX выполняет собственную проекцию и извлечение, сборку набора прикладных PCM, один проход Palladio и сохранение двух вариантов прогноза. Включены запуск интерпретатора/JVM/Maven, чтение, построение и сериализация. Сбор телеметрии, общая подготовка ролей, передача, копирование и установка инструментов находятся до этой границы. Это измерение вычислительного конвейера восстановления, а не отношения времени всего наблюдения системы.

Все дополнительные проверки обоих методов выполнены **после всех девяти измерительных пар соответствующего приложения**: отдельное повторное графовое построение, второй проход Palladio, восемь контролей с известной вероятностью и сверка с исходными сохранёнными прогнозами. Все проверки пройдены; точки, границы и статусы воспроизведены с допуском 10⁻¹². Их 57 отдельных записей ресурсов сохранены вне основного времени. Это воспроизведение прогнозов каждого метода, а не утверждение, что Gstar и PMX всегда дают одну вероятность.

Время ниже — медиана трёх повторов. Коэффициент PMX/наш сначала вычислен внутри каждой пары, затем взята медиана; он может отличаться от отношения приведённых медиан времени. CPU — сумма неперекрывающихся команд, память — максимальный записанный пик процесса и его потомков, затем медиана повторов; это не суммарная память виртуальной машины.

| Приложение | Попытки | Пар | Наш конвейер, с | PMX/Palladio, с | PMX / наш, × | CPU наш / PMX, с | Память наш / PMX, MiB |
| --- | ---: | ---: | ---: | ---: | ---: | --- | --- |
| DeathStarBench | 3600 | 3/3 | 2,421 | 167,907 | 69,367 | 2,950 / 245,830 | 208,223 / 2389,809 |
| DeathStarBench | 7200 | 3/3 | 4,127 | 191,862 | 46,047 | 4,700 / 279,480 | 306,637 / 3608,156 |
| DeathStarBench | 14 400 | 3/3 | 7,885 | 249,690 | 31,731 | 8,440 / 373,460 | 521,035 / 4379,801 |
| OpenTelemetry | 3600 | 3/3 | 3,224 | 178,677 | 55,431 | 3,620 / 236,290 | 444,691 / 1852,469 |
| OpenTelemetry | 7200 | 3/3 | 5,980 | 264,828 | 44,503 | 6,440 / 398,910 | 787,871 / 2591,586 |
| OpenTelemetry | 14 400 | 3/3 | 12,093 | 586,017 | 48,766 | 12,580 / 1004,320 | 1473,699 / 4355,898 |
| Petclinic | 3600 | 3/3 | 1,772 | 177,718 | 100,239 | 2,280 / 206,000 | 234,203 / 1604,094 |
| Petclinic | 7200 | 3/3 | 2,974 | 195,375 | 65,819 | 3,460 / 235,800 | 366,203 / 2293,723 |
| Petclinic | 14 400 | 3/3 | 5,276 | 251,446 | 47,818 | 5,770 / 327,160 | 630,676 / 3798,812 |

Наш конвейер быстрее во всех 27 парах; диапазон индивидуальных коэффициентов 31,079–100,339×, медиан девяти сочетаний — 31,731–100,239×. При четырёхкратном объёме отношение медиан нашего времени к базовому составляет 3,258/3,751/2,978, памяти — 2,502/3,314/2,693 для DeathStarBench/OpenTelemetry/Petclinic. У PMX время возрастает в 1,487/3,280/1,415 раза. Поэтому меньший абсолютный расход нашего конвейера не означает меньшего относительного роста во всех приложениях. Вывод о росте по числу сервисов из этой серии не следует.

В измеренный PMX включён исходный запускатель с фиксированными 20 секундами на операцию. Вычитание этой составляющей в CSV — арифметическое разложение, а не измерение оптимизированной реализации. Наш таймер включает расчёт всех графовых вариантов, поэтому не занижает затраты одного Gstar в данной реализации. Коэффициенты характеризуют зафиксированные интеграции и входы, а не универсальное преимущество над любым использованием Palladio. Внутри пары оба метода выполнялись последовательно на одном GitHub Actions runner, с чередованием порядка; отдельный runner выделен каждому приложению, его фактические CPU/память сохранены в environment.json.

[Полный вычислительный отчёт](../tables/v4-pipeline-benchmark-34708991534/README.md) содержит девять строк с минимумами и максимумами, все 27 исходных пар и все 18 строк масштабирования. Отдельные CSV сохраняют каждую команду основного времени, CPU/RSS, дополнительные проверки, объёмы входов, число spans, категорий и моделей. Все семь табличных файлов байт в байт совпадают с удалённым производителем **34712531566** ([receipt](../evidence/v4-pipeline-tables-34712531566/retention.json)).

[Прежний эксперимент полного конвейера](V3_PIPELINE_PERFORMANCE_RESULT.md) с базовыми отношениями 50,68/48,03/63,71 остаётся результатом прежней привязки и иной границы, включавшей дополнительные проверки. Изменение коэффициентов между сериями нельзя приписывать одному исправлению привязки. Новые цифры 1,84–9,84 для CUDD/aGrUM/Storm относятся к построению вычислительного представления по уже идентифицированной модели; они также не смешиваются с этим измерением восстановления.

## Неполная наблюдаемость

Запуск **34707544832** использует ту же независимо проверенную test-совокупность: 60 плановых случаев, 57 поддержанных, 61 200 поддержанных попыток. Записи не отбираются по совпадению E=Y. Заданы шесть уровней пропуска (0, 10, 25, 50, 75, 100%), три механизма и пять вариантов раскрытия сведений — **5130 строк**. Исходные неизвестности сохраняются. Маскируются наблюдённые native-координаты при фиксированной структуре и внешнем условии срока, а не строится заново топология из прореженных трасс.

В следующей таблице показана ширина эмпирических идентификационных границ, в п.п., с объединением счётчиков по попыткам каждого приложения. Однозначная доля равна `100% − ширина`, поскольку индивидуальное событие бинарно. В каждом уровне и механизме бюджет скрытых координат одинаков; структура пропусков отличается.

| Механизм и доля скрытых координат, без раскрытия | DeathStarBench | OpenTelemetry | Petclinic |
| --- | ---: | ---: | ---: |
| Исходные наблюдения, 0% дополнительных пропусков | 7,678 | 14,023 | 0,000 |
| Равномерные координаты, 50% | 90,111 | 84,255 | 88,343 |
| Координаты с повышенной потерей при отказе, 50% | 90,056 | 84,519 | 89,287 |
| Потеря native-записи целой попытки, тот же бюджет 50% | 51,561 | 49,523 | 49,500 |
| Все наблюдённые native-координаты скрыты, 100% | 95,633 | 85,000 | 99,019 |

Совместимость с первичными исходами равна 100% во всех настройках, включая механизм, связанный с отказами. При таком маскировании множество допустимых состояний расширяется, поэтому сохранение исходной совместимости ожидается из вложенности множеств. Это проверка корректности реализации, а не новое независимое подтверждение адекватности. Рост ширины до 84–90 п.п. при половине отдельных координат означает существенную потерю информативности. Целые сохранённые попытки полезнее разрозненных оставшихся координат для конъюнктивного события в этих случаях. При полностью скрытых native-наблюдениях срок ещё исключает часть попыток; потому границы не обязательно равны `[0,1]`.

Для генерации искусственного механизма потерь, связанных с отказами, Y используется только генератором маски. Ядро расчёта и правило раскрытия сведений Y не получают. Это контролируемое исследование последствий заданной потери информации, не обучаемая стратегия наблюдения.

| Возврат сведений после 50% потерь, связанных с отказом | DeathStarBench: раскрыто координат / устранено неоднозначностей | OpenTelemetry | Petclinic |
| --- | ---: | ---: | ---: |
| Выбор и приём реплики | 13 379 / 441 | 27 671 / 496 | 15 202 / 745 |
| Результат входа | 9179 / 961 | 11 590 / 219 | 10 731 / 2352 |
| Обязательные завершения | 49 162 / 3247 | 59 910 / 1683 | 23 979 / 4301 |
| Все искусственно скрытые native-сведения | 71 720 / 14 828 | 99 171 / 15 227 | 49 912 / 19 286 |

Возврат всех скрытых сведений точно восстанавливает исходную ширину 7,678/14,023/0 п.п., но не создаёт ранее отсутствовавших наблюдений. Бюджеты групп различаются; эта таблица не доказывает оптимальности выбора завершений и не оценивает стоимость физического инструментария. [Полная таблица всех уровней, механизмов, групп, затронутых попыток и ширин](../tables/v4-missingness-v2-34707544832/all-case-settings.csv) и [сводка по приложениям](../tables/v4-missingness-v2-34707544832/application-summary.csv) сохраняют все настройки.

Эти границы описывают определимость вероятности при зафиксированном эмпирическом законе наблюдений. Они не являются доверительными интервалами вероятности успеха будущей выборки. Обнаруженная широкая неопределённость — существенная граница применимости, которую следует включать в вывод наряду со скоростью вычисления.

## Воспроизводимость и итоговая формулировка

Полный путь от первичных записей до состояния и старого/исправленного события воспроизводится из [компактной первичной капсулы](V4_SINGLE_PRIMARY_ATTEMPT_V1_RESULT.md) одной удалённой командой. Все реальные построения моделей, новые кампании, native-измерения и статистические пересчёты выполнены в GitHub Actions. Локально сохранены проверенные компактные результаты и полные таблицы, без скачивания полных когорт.

Все исходные артефакты нового научного запуска, отдельного агрегатора и первых измерений пропусков/производительности сохранены как **310 исходных ZIP, 718 849 377 байт**, в неопубликованном release **387645995**, архивный запуск **34708008168**. [Проверенный архивный receipt](../evidence/v4-confirmation-complete-archive-34708008168/retention.json) сверяет точный census, SHA конфигурации производителя и текущие SHA/размеры draft assets. Отдельный первоначальный технически неудачный сбор сохранён как 167 ZIP, 721 038 678 байт, release 387627401. Прежние три первичных аудита сохранены как 21 ZIP в release 387606839.

Новый парный конвейер **34708991534** и его табличный производитель **34712531566** сохранены отдельно: все **7 исходных ZIP, 46 803 348 байт**, неопубликованный release **387672616**, архивный запуск **34712710619**. [Архивный receipt](../evidence/v4-pipeline-complete-archive-34712710619/retention.json) подтверждает полный состав, SHA оригинальных файлов и текущих assets. Архив включает полные построенные модели, исходные и повторные ответы, журналы и ресурсы, а не только компактные таблицы. Локально полные native-когорты не загружались.

Удалённый производитель полных таблиц основной доработки — **34708006475**; все 38 табличных файлов совпадают с локальной выгрузкой байт в байт ([receipt](../evidence/v4-refinement-tables-34708006475/retention.json)). Дополнительная сверка 54 готовых Gstar/B0-прогнозов добавлена отдельно после этого производителя и не приписывается ему. Семь таблиц нового парного конвейера сверены отдельно с производителем 34712531566. 356 исходных научных блокировок, 14 блокировок подтверждения и девять блокировок технической поправки сохранены. v3 в названиях дополнительных workflow обозначает исправление входа/сводки, а не третий научный алгоритм.

**Формулировка результата:** метод автоматически строит формальную модель успешности операций из телеметрии с сохранением совместных наблюдений и возвращает точку, обоснованные границы либо явный отказ применимости. После обоснованного исправления привязки модель прошла независимую первичную проверку; на общей поддержке прогнозная ошибка ниже G0 и сопоставима с рассмотренными PMX/B0, без установленного превосходства над ними. Специализированное вычисление сохраняет точный ответ и снижает стоимость первого построения относительно рассмотренных общих решателей; выгода зависит от режима повторного использования. В отдельном парном опыте вычислительный конвейер восстановления по подготовленной телеметрии требует меньше времени, CPU и памяти, чем зафиксированная интеграция PMX/Palladio, при сохранении исходных прогнозов каждого метода; измерение включает расходы их запусков. Неполная наблюдаемость может резко ограничивать информативность даже быстро и корректно вычисленных границ.
