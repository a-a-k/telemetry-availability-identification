# Диагностика исправленной привязки на открытых исторических данных

**На 29 заранее выбранных исторических кампаниях кандидат исправления устранил 535 противоречий исходной привязки: 0 несовместимых интервалов на 103 200 попытках с поддерживаемой моделью.** Это не независимое подтверждение и не результат проверки прогноза на новых данных.

[Запуск 34696809884](https://github.com/a-a-k/telemetry-availability-identification/actions/runs/34696809884) завершён успешно, source `1e1a443d73dcfa2a529aa952359153f93101f34d`. [Обоснование и математическая область применимости](V4_BINDING_REVISION_V1.md); фиксированный список — `configs/v4_binding_diagnostic_v1.json`.

Изменения следуют из первичных свидетельств: eligibility относится к возможности доставить вызов реплике в пределах операции и получает положительное подтверждение по native receiver, а завершение объявленного Petclinic POST retry относится к логической группе. Старые пробы сохранены совместно как дополнительный контекст. Внешняя semantic_success не используется привязкой; инверсия всех таких меток не изменила ни одну построенную модель.

| Приложение | Кампании | Все запланированные попытки | Попытки с моделью | Противоречия старой привязки | Исключённые успехи новой | Исключённые отказы новой | Новые точечные / интервальные модели |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| DeathStarBench | 4 | 14 400 | 13 200 | 102 | 0 | 0 | 10 / 1 |
| OpenTelemetry Demo | 4 | 14 400 | 14 400 | 47 | 0 | 0 | 9 / 3 |
| Petclinic | 21 | 75 600 | 75 600 | 386 | 0 | 0 | 82 / 2 |
| Всего | **29** | **104 400** | **103 200** | **535** | **0** | **0** | **101 / 6** |

Один прежний структурный отказ DS сохранён; его 1200 попыток входят в общий учёт. Все поддерживаемые попытки проверены в обоих направлениях. Полностью известное состояние имеется у 45 311 попыток; в остальных могут оставаться неизвестными, например, возможности невыбранной реплики. Неизвестность состояния не обязательно означает неизвестность результата: только 1812 попыток имеют непостоянный E на допустимых дополнениях.

В Petclinic **10** успешных повторов распознаны по последовательности 503 → 201 и одному наблюдаемому receiver второй попытки. **Один** случай оставлен неизвестным из-за непроверенного порядка или идентичности попыток. Ещё 896 локальных исходов retry-группы не полностью наблюдаемы; другие известные условия обычно уже устанавливают отказ всей операции. Поэтому неполная локальная информация не создаёт 896 неопределённых внешних результатов: в Petclinic неопределённы только две попытки.

Для `create_visit`, split/NC/r0, граница новой версии — **[835/900, 836/900]**, зарегистрированная частота **835/900**. Для split/NC/r7 — **[820/900, 821/900]**, частота **821/900**. В обоих случаях неопределённость сохранена, а не устранена подстановкой внешнего исхода. Первичная независимая проверка persistence исторических записей остаётся ограниченной отсутствием исходных SELECT-строк; эти числа относятся к зарегистрированным исходам.

## Вычислительная проверка новой постановки

На всех **107** новых поддерживаемых моделях проверены исходное представление и общая проекция завершений. В каждом — оба наших алгоритма, CUDD fixed/sift, aGrUM conditioned/joint и Storm sparse/compact: **1712 запросов**, в каждом совпали все **11 пар точных границ и статусов**. Дополнительно проверены **2916 концов свидетельств** на соответствие маске и значению функционала.

Это подтверждает применимость текущего сокращения и эквивалентность реализованных точных представлений к новым построенным моделям в объявленном семействе масок. Диагностический запуск не сравнивает производительность. Старые измерения стоимости сохраняют прежнюю область применимости; новые коэффициенты ускорения из этого запуска не выводятся.

[Полная таблица 108 случаев](../tables/v4-binding-diagnostic-v1-34696809884/attempt-compatibility.csv), [все 1712 проверок решателей](../tables/v4-binding-diagnostic-v1-34696809884/exact-qualification.csv), [сводка](../tables/v4-binding-diagnostic-v1-34696809884/summary.json), [происхождение компактных артефактов](../evidence/v4-binding-diagnostic-v1-34696809884/retention.json). Полные модели и покомпонентные записи попыток сохранены в удалённых full artifacts.

Следующий обязательный этап — ограниченная новая подтверждающая серия с заранее сохранёнными численными прогнозами, включая PMX и B0, и первичными доказательствами эффектов записи. До неё не заявляются независимая адекватность исправления, прогнозное превосходство или готовность исследования к публикации. Историческое C12 остаётся в отчёте.
