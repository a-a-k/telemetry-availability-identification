# Исправление неприменимой таблицы переноса после завершения научных стадий

В запуске `34703686592` все 18 сборов, 18 построений графовых прогнозов, 18 извлечений PMX, численный анализ Palladio, 18 строгих фиксаций прогнозов, 18 расчётов realized-state границ, 18 оценок прогнозов и 18 независимых первичных проверок завершились успешно. Последняя стадия `analysis` упала: старый `transfer_census` без проверки предполагал наличие обоих размещений и обратился к отсутствующему `('deathstarbench_social_network', 'colocated', 'N', 0)`.

Новая серия с самого начала включает только `split`, законы N/NC и три повтора. Перенос между размещениями не входит в неё. Исправление возвращает для этой таблицы **not_planned**, ноль запланированных переносов и пустой census. Оно не подставляет данные другого размещения, не удаляет неудачные попытки и не меняет прогнозы, оценки ошибок, bootstrap, первичные исходы или их границы. Прежняя таблица переноса полной исторической серии сохраняется отдельно.

Сводка строится удалённо заново из **сохранённых** frozen/evaluation/primary-compact артефактов. Source run/head в сводке указывают на исходные измерения; фактические run/head нового агрегатора записываются отдельно как `analysis_run` и `analysis_head`. Общий статус исходного запуска остаётся `failure`. Новых наблюдений, построения моделей, решения PMX или открытия исходов до прогнозов не происходит.

В независимом аудите хронологии последний из 18 forecast-freeze завершился в **16:49:12 UTC**, первый bounds-job начался в **16:49:14**; последний bounds-job завершился в **16:50:19**, первый outcome-job начался в **16:50:21** (2026-09-12). Все научные стадии сохранены. Исторический C12 остаётся отдельным отклонением исходного исследования.

Первичные результаты: 64 800/64 800 исходов проверены независимо, ноль расхождений с зарегистрированными метками. На 61 200 поддержанных попытках нет несовместимости ни в одном направлении; 3600 попыток имеют структурный отказ, 4411 поддержанных попыток остаются неоднозначными. Все 48 дополнительных контролей совместимы, включая 24 принудительных 503→201 retry. Эти числа переписаны из сохранённых компактных результатов, не пересчитаны локальным исполнением модели.

Последующие workflow с пропусками и затратами теперь проверяют **успешное завершение всех научных стадий**, допуская ровно этот документированный отказ сводки. Для каждой из семи матричных стадий нужны все 18 успехов; любой другой отказ блокирует вход. Это изменение допуска по техническому статусу, а не ослабление требования к ответам или выборке. Оба пропущенных автоматических запуска v2 (`34706518746`, `34706518750`) не выполнили ни одного измерения и остаются в истории.

Уровни и механизмы пропусков, их seed, все восемь вычислительных конфигураций, два представления, семь фаз, три повтора и исходный fair worker остаются прежними. Вариант v3 обозначает только новый вход и сборку отчёта; новых научных алгоритмов он не добавляет. В частности, несовпадение E с независимым Y не является критерием отбора записей в эксперимент с пропусками.
