# Final comparison method bindings v1; full pipeline admission pending

The [author's v3](SIMPAT_CORRECTION_V3.md) governs this selection. These method definitions are chosen after explicitly opened development qualification and before any prospective main acquisition. Main remains0/240. Freezing an algorithm does not admit an unfinished acquisition/PMX/evaluator workflow. The final executable lock and full-duration preflight must qualify that chain before main dispatch.

| Reported method | Saved calculation / identification | Actual calibration information |
| --- | --- | --- |
| Gstar | `graph-observed-execution-joint-v1`, execution functional, source-bound v2 observations; prospective metadata/timing wrapper v1 | Native graph and parent/CLIENT relations, explicit replica identities, declared source contracts, joint request-aligned proxy eligibility, mandatory selected-replica footprint, native completion certificates, whole external duration |
| GID | Same discovered graph/replicas and joint masked law; reachability functional | Graph/declarations and proxy eligibility; other observation coordinates marginalized by the functional |
| Gselected | Reachability plus all mandatory selected replicas eligible | Same plus joint demand footprint |
| G_without_deadline | Gstar with T removed | Same except external duration has no effect on this functional |
| G_without_selection | Gstar with mandatory selected-replica guard removed | Same except demand coordinates have no effect on this functional |
| G_without_completion | Gstar with native call-completion guard removed | Same except completion coordinates have no effect on this functional |
| G0 | `G0-AINA-fixed-count-ordinary-v1`, exact source-adapted fixed-k graph integration | Shared graph/replicas/declarations; all-attempt mean failed eligibility fraction and its mask bounds; no demand/completion/duration/business labels |
| B0 | `B0-v3-raw-calibration-frequency-csv-v2`, raw S/N | Every external calibration attempt and its semantic verdict, including timeouts; no smoothing/native/health |
| PMX | Primary `conditional_local`, caller-context PMX/PCM/Palladio route | Physically separate copy of the same native and external-calibration request pool; declared runner identity; no proxy health or our graph/parameters |
| PMX_inclusive | Same independent model route, inclusive failure composition | Same PMX inputs; additional comparator only |

Gstar is one execution refinement of GID, with separate selection/completion/deadline ablations. This does not introduce three new causal hypotheses. The confirmed H-EXEC experiment supports its scoped routing/execution motivation; it neither proves all business-equivalence assumptions nor explains AINA's historical error. No second deep H is selected. Gselected is an intermediate ablation, not a duplicate of GID or Gstar. A shared calculation that yields equal forecasts in a particular campaign does not imply identical methods.

The [S3/I5 specification and proofs](GRAPH_EXECUTION_MODEL_V1.md) define the model class, conditional semantic claim and sharp masked-law functionals. They distinguish population identification from finite-sample estimation and standard indicator/partial-identification facts from the applied contribution. Native completion certificates, no-error flags, physical capability and full payload/persistence correctness are distinct. Source-declared mandatory/optional relations, cached branches, non-target eligibility fixed true, maximum2s probe age, native identity resolution, DB peer completion and2s external deadline retain the [qualified v2 scope](V3_APPLICATION_EXECUTION_V2.md). No residual q, ML fit, independent-coordinate factorization or target outcomes identify this model.

The prospective wrapper keeps v2's numeric observation/solver path, changes the declared expected calibration census to1200 per DeathStar/OTel operation and900 per Petclinic operation, records the actual campaign/data role, and separates extraction, identification and solve timers. Bounded controls require the full numeric result hash to match v2 for identical artificial observations. Calibration quality errors yield explicit failed/unsupported slots; partial masked observations are not silently completed. Source-required graph absence is unsupported. Ambiguous functionals retain exact bounds and a null point. Some ablations can be ambiguous while Gstar is point-identified. Low forecast error is never a qualification gate.

G0 is an explicit adaptation, not unchanged AINA or ICSE: its scenario parameter, eligible pool, operation target and exact integration are declared in [the adaptation specification](G0_AINA_ORDINARY_V1.md). The original source's quantization and its discontinuity remain. Technical qualification gives10/10 points and41 structural controls; this is independent of predictive validation. The two Petclinic Gstar calibration discrepancies of-13/900 remain in [the result](milestones/V3_APPLICATION_EXECUTION_V2_RESULT.md); they do not justify compensating calibration or changing the event.

PMX's primary variant is chosen by propagated/local failure semantics, not by favorable errors. Qualified native operation policies remain DeathStar `service_boundary`, OTel `explicit_server`, Petclinic `server_and_observed_db_client` with its observed MySQL endpoint adapter. Every policy receives the same available whitelisted native pool; actual retained/discarded spans and manual work are reported. OTel's server boundary aggregates DB execution within enclosing service operations and does not create separate Redis/PostgreSQL peer components. Petclinic's explicitly observed JDBC CLIENT peers are not synthetic native SERVER spans or independent DB state measurements. External wrappers encode the complete calibration business verdict; their enclosing duration is a structural adapter value, not a fitted response-time observation. Full caller-context operation identities, failure-type containment and integer loop PMFs are explicit source-adapter changes. The pinned PMX jar and qualified Palladio analyzer, source locks and known-probability controls remain required. Unsupported conditional positivity/propagation stays absent even if inclusive succeeds.

The three physical acquisition exports are ordinary calibration, PMX calibration and closed evaluator. Only the acquisition/preparation job reads raw Docker/controller state or all periods. Public acquisition receipts contain hashes and identity, without test verdicts. Builder input hashes and identities must match the receipt. Saved candidates, bounds and absences are sealed before evaluator download; evaluator role digest must equal the frozen receipt. Hashes provide integrity/association; fresh workflow jobs and actual read audits provide the input separation. They are not claims of adversarial confidentiality. Full native/model payloads remain remote, retained90 days even on failure; locally retained results use explicit compact artifact/member allowlists.

Primary test analysis includes every attempt. Additional stable membership requires known identical a/b eligibility across the request expanded by1s at both ends, observations bracketing both ends, and no gap above2s. This is observed probe stability, not physical stationarity. Missing or changing probes affect only this additional membership. All methods retain the same unconditional calibration forecasts in that view; they are not refitted to selected test rows. Empty stable subsets keep their planned coverage and have no error estimate.

The primary analysis keeps the six frozen Gstar−G0/PMX contrasts, paired campaign clusters, equal operation/condition weights,10000 resamples and99.1666666667% intervals. Additional stable summaries are descriptive points; no unregistered significance family is introduced. Missing acquisition, builder, solver or evaluator jobs remain in the full planned census. Unqualified evaluator campaigns retain counts/forecasts but do not enter scored support. Forecast error and test native/health coverage cannot qualify the primary event.

Source-only changed-placement transfer is not identified by these current empirical joint-law bindings. Every planned direction/operation/method is recorded as unsupported with null point/error and unrestricted target business-event bounds[0,1]. For an available source point p, the corresponding change enclosure is[-p,1-p]. These are no-information identification enclosures, not confidence intervals or evidence of useful transfer accuracy. Target outcomes only report observed target/change after candidate freeze. No target calibration/health enters a transfer forecast. Zero point coverage is a substantive limitation to publish, not an unfinished hidden estimator or a successful transfer claim.

Costs retain shared acquisition and native/graph extraction once, explicit marginal B0/G0/PMX stages, process wall/CPU/RSS and byte counts. Shared joint graph solving computes all ablations in one pass; its total is not multiplied across methods. Process totals overlap stage sums and are not added to them. Reapplication means a fresh build from a new calibration window; incremental update, historical integration labor, monitoring off/on overhead and scalability are unmeasured until an explicit measurement exists. Their fields are null with reasons, not zero. No monitoring-overhead or scalability claim is admitted.

The intended full-duration preflight uses a distinct771603 namespace and six application×placement NCD/r0 cases, the same60/900/900s windows and4operations/s as main. It is development evidence and contributes no independent main campaigns. The future main771601 namespace remains240 campaigns and800 operation cells per method. All operations, every absence and all thirty author-checklist criteria must be reconciled before publication.
