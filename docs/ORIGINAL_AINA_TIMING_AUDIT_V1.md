# Original AINA timing feasibility audit v1

Descriptive extension of successful original full audit34231677146. Original files remain unchanged. Before reading their error strings, freeze an all25000-window census of timeout categories and conditional duration bounds. The original sequential100-probe loop uses requests timeouts6s, receives --window40 without using that argument, starts after a nominal15s delay, while controller sleep holds faults60s after its preparation/stop phase. No per-probe timestamps were retained.

Count only errors containing both `Read timed out.` and exact `(read timeout=6)` or `(read timeout=6.0)` as quantified read timeouts. All other timeouts, HTTP5xx/4xx, empty JSON, missing order ID and unknown errors remain separately counted. At most one final timeout contributes per failed sequential probe. Conditional on logged timeouts reflecting the configured requests read timer, q such probes require at least6q seconds. Report strict thresholds6q>45 (nominal remaining interval ignoring controller overhead) and6q>60 (declared sleep duration). Preserve every window, histogram, success denominator and error type; no adverse windows are excluded or relabeled.

These are feasibility diagnostics, not actual timestamps: unknown preparation/stop/start durations shift and extend the actual physical fault interval. A long probe loop does not identify which attempts occurred after recovery, the number of affected successes or a causal mediation fraction. No second H/refinement is selected or confirmed by this audit. Original model semantics, forecasts and successful full audit are untouched. Fixed-family MC diagnostic is not rerun because this extension does not change it.

Same exact unpublished draft source asset550517536 and250 nested original hashes. One remote GitHub Actions job, Python3.13.15, maximum10minutes, contents:write only for draft GET visibility; checkout does not persist credentials, no release mutation. Compact counts/summary only downloaded locally,90-day retention including failures. Original native/model/probe payloads remain remote. Artificial tests cover strict threshold boundaries, unrelated timeout strings and fast HTTP failures. New diagnostic code/config/source locks committed before dispatch. After completion, interpret conditionally and update all30 v3 criteria.

Requests documents that the read timeout measures an interval without received socket bytes, not an overall request deadline: [official Quickstart](https://requests.readthedocs.io/en/latest/user/quickstart/#timeouts). The old package version was not pinned; diagnostic conclusions remain conditional on that documented behavior.
