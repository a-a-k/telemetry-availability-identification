name: M9U PMX external-request conformance
'on':
  workflow_dispatch: null
permissions:
  contents: read
  actions: read
jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
    - uses: actions/checkout@v7
    - uses: actions/setup-python@v7
      with:
        python-version: 3.13.15
    - uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '11'
    - name: Validate frozen request controls
      run: |
        python -m pip install -r requirements.txt
        python -m pip install --no-deps -e .
        python -m telemetry_availability.pmx_request_conformance validate
    - name: Obtain pinned PMX author binary and options
      env:
        AUTHOR_BASE: https://se-gitlab-extern.fzi.de/SebastianWeberFZI/ableitung-von-leistungsmodellen-in-ci/-/raw/9b8d4c5707751eeabe31f7e7d6b7de0acf0c45a2
      run: |
        mkdir -p workflow-input/m9u
        curl --fail --location --retry 8 --retry-all-errors "$AUTHOR_BASE/main.jar" --output workflow-input/m9u/main.jar
        curl --fail --location --retry 8 --retry-all-errors "$AUTHOR_BASE/Options.txt" --output workflow-input/m9u/Options.txt
    - name: Verify accepted bridge and preserve the artificial input contract
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        python -m telemetry_availability.pmx_request_conformance prepare --inputs workflow-input/m9u --options workflow-input/m9u/Options.txt --out workflow-results/m9u-input
    - uses: actions/upload-artifact@v7
      with:
        name: m9u-request-input-${{ github.run_id }}
        path: workflow-results/m9u-input
        if-no-files-found: error
        retention-days: 90
    - name: Extract six cases twice and retain both error parameterizations
      run: |
        mkdir -p workflow-results/m9u-contract
        /usr/bin/time -v -o workflow-results/m9u-contract/extraction-resource-usage.txt python -m telemetry_availability.pmx_request_conformance execute --contract workflow-results/m9u-input --jar workflow-input/m9u/main.jar --out workflow-results/m9u-contract
    - uses: actions/upload-artifact@v7
      if: ${{ always() }}
      with:
        name: m9u-request-contract-${{ github.run_id }}
        path: workflow-results/m9u-contract
        if-no-files-found: error
        retention-days: 90
  palladio_solver:
    name: Solve every qualified request-boundary control
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
    - name: Check out study repository
      uses: actions/checkout@v7
    - name: Check out pinned Palladio reliability source
      uses: actions/checkout@v7
      with:
        repository: PalladioSimulator/Palladio-Analyzer-Reliability
        ref: a694e570afb705dc9e0470dc321e77b7219dcea4
        path: palladio-source
    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: 3.13.15
        cache: pip
        cache-dependency-path: requirements.txt
    - name: Set up pinned Java
      uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '17'
    - name: Install dependencies and validate the frozen contract
      run: |
        python -m pip install --upgrade pip
        python -m pip install -r requirements.txt
        python -m pip install --no-deps -e .
        python -m telemetry_availability.pmx_request_conformance validate
        test "$(git -C palladio-source rev-parse HEAD)" = \
          "a694e570afb705dc9e0470dc321e77b7219dcea4"
    - name: Download the retained artificial PCM contract
      uses: actions/download-artifact@v8
      with:
        name: m9u-request-contract-${{ github.run_id }}
        path: workflow-input/m9u-contract
    - name: Reconstruct and audit the accepted release-date target platform
      run: |
        m9u_maven_repo="${RUNNER_TEMP}/m9u-maven-repository"
        evidence_dir="workflow-results/m9u-solver/target-lock/repository"
        mkdir -p "$m9u_maven_repo" "$evidence_dir"
        coordinate="$(jq -r '.target_platform_lock.coordinate' configs/m9a_palladio_bootstrap.json)"
        mvn -B -ntp "-Dmaven.repo.local=${m9u_maven_repo}" \
          dependency:get "-Dartifact=${coordinate}"
        target_file="${m9u_maven_repo}/org/palladiosimulator/palladio-target-platforms/0.1.0/palladio-target-platforms-0.1.0-palladio-2023-03.target"
        repository_url="$(jq -r '.target_platform_lock.pinned_repository_url' configs/m9a_palladio_bootstrap.json)"
        while IFS= read -r relative_path; do
          mkdir -p "${evidence_dir}/$(dirname "$relative_path")"
          curl --fail --location --retry 5 --retry-all-errors \
            --output "${evidence_dir}/${relative_path}" \
            "${repository_url}${relative_path}"
        done < <(jq -r '.target_platform_lock.repository_evidence[].relative_path' configs/m9a_palladio_bootstrap.json)
        python -m telemetry_availability lock-palladio-target-platform \
          --config configs/m9a_palladio_bootstrap.json \
          --target-file "$target_file" \
          --repository-evidence-dir "$evidence_dir" \
          --out workflow-results/m9u-solver/target-lock/manifest.json
        echo "M9U_MAVEN_REPO=${m9u_maven_repo}" >> "$GITHUB_ENV"
        git -C palladio-source diff --exit-code
    - name: Reconfirm the accepted analyzer capability boundary
      run: python -m telemetry_availability audit-palladio-capability-source --config
        configs/m9b_palladio_semantic_controls.json --analyzer-checkout palladio-source
        --out workflow-results/m9u-solver/capability-manifest.json
    - name: Overlay only the audited generic M9U harness
      run: |
        test_bundle="palladio-source/tests/org.palladiosimulator.reliability.tests"
        cp palladio/harness/META-INF/MANIFEST.MF "$test_bundle/META-INF/MANIFEST.MF"
        cp palladio/harness/src/org/palladiosimulator/reliability/tests/PmxPmfBridgeTest.java \
          "$test_bundle/src/org/palladiosimulator/reliability/tests/PmxPmfBridgeTest.java"
        git -C palladio-source status --short > workflow-results/m9u-solver/harness-overlay.txt
        test "$(git -C palladio-source diff --name-only)" = \
          "tests/org.palladiosimulator.reliability.tests/META-INF/MANIFEST.MF"
        test "$(git -C palladio-source ls-files --others --exclude-standard)" = \
          "tests/org.palladiosimulator.reliability.tests/src/org/palladiosimulator/reliability/tests/PmxPmfBridgeTest.java"
    - name: Build the pinned analyzer without running the aligned experiment
      run: |
        mkdir -p workflow-results/m9u-solver/runtime
        java -version > workflow-results/m9u-solver/runtime/java-version.txt 2>&1
        mvn -version > workflow-results/m9u-solver/runtime/maven-version.txt 2>&1
        lscpu > workflow-results/m9u-solver/runtime/runner-cpu.txt
        free -b > workflow-results/m9u-solver/runtime/runner-memory.txt
        set -o pipefail
        /usr/bin/time -v -o workflow-results/m9u-solver/runtime/build-resource-usage.txt \
          xvfb-run -a mvn -B -ntp "-Dmaven.repo.local=${M9U_MAVEN_REPO}" \
            -DskipTests=true -f palladio-source/pom.xml clean verify \
          2>&1 | tee workflow-results/m9u-solver/runtime/build.log
        test "${PIPESTATUS[0]}" -eq 0
    - name: Solve twenty-two models twice and retain every exception
      env:
        TAID_PALLADIO_ALIGNED_ROOT: ${{ github.workspace }}/workflow-input/m9u-contract/models
        TAID_PALLADIO_RESULT: ${{ github.workspace }}/workflow-results/m9u-solver/raw-result.json
        TAID_REPEAT_RUNS: '2'
        TAID_EXPECTED_MODEL_COUNT: '22'
        TAID_EXPECTED_CASE_COUNT: '22'
        TAID_PROBABILITY_TOLERANCE: 1e-12
      run: |
        set -o pipefail
        /usr/bin/time -v -o workflow-results/m9u-solver/runtime/solve-resource-usage.txt \
          xvfb-run -a mvn -B -ntp "-Dmaven.repo.local=${M9U_MAVEN_REPO}" \
            -f palladio-source/pom.xml verify \
          2>&1 | tee workflow-results/m9u-solver/runtime/solve.log
        test "${PIPESTATUS[0]}" -eq 0
    - name: Collect test reports
      if: ${{ always() }}
      run: |
        report_root="palladio-source/tests/org.palladiosimulator.reliability.tests/target/surefire-reports"
        if [[ -d "$report_root" ]]; then
          cp -R "$report_root" workflow-results/m9u-solver/surefire-reports
        fi
    - name: Retain raw untuned solver output and timing
      if: ${{ always() }}
      uses: actions/upload-artifact@v7
      with:
        name: m9u-request-solver-${{ github.run_id }}
        path: workflow-results/m9u-solver
        if-no-files-found: error
        retention-days: 90
  census:
    needs:
    - prepare
    - palladio_solver
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
    - uses: actions/checkout@v7
    - uses: actions/setup-python@v7
      with:
        python-version: 3.13.15
    - name: Install and validate the frozen control
      run: |
        python -m pip install -r requirements.txt
        python -m pip install --no-deps -e .
        python -m telemetry_availability.pmx_request_conformance validate
    - uses: actions/download-artifact@v8
      with:
        name: m9u-request-contract-${{ github.run_id }}
        path: workflow-input/m9u-contract
    - uses: actions/download-artifact@v8
      with:
        name: m9u-request-solver-${{ github.run_id }}
        path: workflow-input/m9u-solver
    - name: Retain all missing error and numeric outcomes under the fixed oracles
      run: python -m telemetry_availability.pmx_request_conformance summarize --contract
        workflow-input/m9u-contract --solver workflow-input/m9u-solver --out workflow-results/m9u-census
    - uses: actions/upload-artifact@v7
      if: ${{ always() }}
      with:
        name: m9u-request-census-${{ github.run_id }}
        path: workflow-results/m9u-census
        if-no-files-found: error
        retention-days: 90
