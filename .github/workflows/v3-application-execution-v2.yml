name: V3 ten-operation graph execution qualification v2
on:
  workflow_dispatch:
permissions:
  contents: read
  actions: read
jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        profile: [deathstarbench_social_network, opentelemetry_demo, spring_petclinic_microservices]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: 3.13.15
      - run: |
          python -m pip install -r requirements.txt
          python -m pip install --no-deps -e .
      - env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p workflow-results/preparation
          /usr/bin/time -v -o workflow-results/preparation/resource-usage.txt python -m telemetry_availability.v3_application_execution_v2 prepare --profile '${{ matrix.profile }}'
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: v3-execution-ordinary-${{ matrix.profile }}-${{ github.run_id }}
          path: workflow-results/ordinary
          retention-days: 90
          if-no-files-found: warn
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: v3-execution-preparation-compact-${{ matrix.profile }}-${{ github.run_id }}
          path: workflow-results/preparation
          retention-days: 90
          if-no-files-found: error
  build:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        profile: [deathstarbench_social_network, opentelemetry_demo, spring_petclinic_microservices]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: 3.13.15
      - run: |
          python -m pip install -r requirements.txt
          python -m pip install --no-deps -e .
      - uses: actions/download-artifact@v8
        with:
          name: v3-execution-ordinary-${{ matrix.profile }}-${{ github.run_id }}
          path: workflow-input/ordinary
      - run: |
          mkdir -p workflow-results/fit
          /usr/bin/time -v -o workflow-results/fit/resource-usage.txt python -m telemetry_availability.v3_application_execution_v2 build --profile '${{ matrix.profile }}'
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: v3-execution-fit-compact-${{ matrix.profile }}-${{ github.run_id }}
          path: workflow-results/fit
          retention-days: 90
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: v3-execution-model-${{ matrix.profile }}-${{ github.run_id }}
          path: workflow-results/models
          retention-days: 90
          if-no-files-found: error
  replay:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        profile: [deathstarbench_social_network, opentelemetry_demo, spring_petclinic_microservices]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: 3.13.15
      - run: |
          python -m pip install -r requirements.txt
          python -m pip install --no-deps -e .
      - uses: actions/download-artifact@v8
        with:
          name: v3-execution-model-${{ matrix.profile }}-${{ github.run_id }}
          path: workflow-input/model
      - run: |
          mkdir -p workflow-results/replay
          /usr/bin/time -v -o workflow-results/replay/resource-usage.txt python -m telemetry_availability.v3_application_execution_v2 replay --profile '${{ matrix.profile }}'
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: v3-execution-replay-compact-${{ matrix.profile }}-${{ github.run_id }}
          path: workflow-results/replay
          retention-days: 90
          if-no-files-found: error
