name: V3 exact backends durable unpublished evidence archive
on:
  workflow_dispatch:
permissions:
  contents: write
  actions: read
concurrency:
  group: v3-exact-backends-34596410909-durable-archive-v1
  cancel-in-progress: false
jobs:
  archive:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - name: Preserve all sixteen original exact backends evidence ZIPs
        env:
          GH_TOKEN: ${{ github.token }}
        run: python scripts/archive_v3_exact_backends_v1.py
      - uses: actions/upload-artifact@v7
        with:
          name: v3-exact-backends-archive-compact-${{ github.run_id }}
          path: |
            workflow-results/exact-backends-archive/compact.json
            workflow-results/exact-backends-archive/manifest.json
            workflow-results/exact-backends-archive/source-artifacts.json
          if-no-files-found: error
          retention-days: 90
