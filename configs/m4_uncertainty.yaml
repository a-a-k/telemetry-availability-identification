schema_version: 1

experiment:
  id: m4_simultaneous_uncertainty
  transfer_config: m3_transfer.yaml
  confidence_level: 0.95
  branch_tolerance: 0.02
  branch_max_nodes_per_domain: 12000
  simulation_episodes: 10000
  local_smoke_max_dataset_fits: 30
